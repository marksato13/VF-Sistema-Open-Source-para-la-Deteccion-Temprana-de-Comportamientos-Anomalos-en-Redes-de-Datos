# Revisión Claude — UDP-25M/R04

Fecha: 6 de agosto de 2026. Dictamen final: **ACEPTAR CON LIMITACIONES**.

Claude autorizó una única ejecución después del preflight continuo. En el cierre leyó en modo sólo lectura manifest, deltas, stop/resumen PCAP, iperf3, EVE, recursos, bundles de hashes, extracción, CSV y ledger. Corroboró 43,195 paquetes sin drops, 62,504,368 bytes y 43,166 datagramas por extremo, cero pérdida/reordenamiento, jitter, catorce registros EVE, tres filas y hashes cruzados. Codex verificó aparte la composición y secuencia binaria del PCAP, control TCP, comparación R01–R03 y auditor global.

Claude abrió el hallazgo H1 porque interpretó `duplicate_feature_vector_count` como coincidencias exclusivamente internas de `train`. Una segunda revisión leyó `find_cross_campaign_duplicate_vectors`: `total` se incrementa para toda coincidencia entre campañas y `cross_partition_total` sólo cuando esa coincidencia además cruza particiones. Confirmó que 25→26 totales y 8→9 cruces es correcto para R02 train↔R04 validation, y cerró H1 sin cambio de código.

La composición protocolaria no se deriva sólo del resumen de longitudes: la confirmó la auditoría binaria independiente de Codex. Se conserva el dictamen limitado por el delta Suricata +4 sin causa, dos flows de preflight diferidos fuera del PCAP/features y el alcance del laboratorio. Claude autoriza exclusivamente el preflight `F1N-UDP-50M-R04`; no su captura ni scoring.
