# Auditoría G0 — 20 de julio de 2026

Auditoría remota de solo lectura ejecutada desde VM01 mediante Ansible a las `17:46 America/Lima`. El estado `CHANGED` mostrado por el módulo `shell` no representa una modificación de configuración.

## Resultados

| Nodo | Zona horaria | NTP | Ruta experimental | SSH |
|---|---|---|---|---|
| Sensor | `America/Lima` | `yes` | DMZ conectada por `ens38` | activo |
| Servidor | `America/Lima` | `yes` | `10.30.0.10` local por `ens38` | activo |
| Kali | `America/Lima` | `yes` | `10.30.0.10 via 10.20.0.1 dev eth1` | activo |
| Cliente | `America/Lima` | `yes` | `10.30.0.10 via 10.20.0.1 dev ens38` | activo |

Kali recuperó sincronización NTP después del reinicio sin requerir una nueva modificación. El hallazgo previo `Packet count: 0` queda cerrado con la comprobación positiva `NTPSynchronized=yes`.

## Condición pendiente

Las interfaces externas continúan activas:

- Sensor: `ens34=172.17.25.111/24`.
- Servidor: `ens34=172.17.25.112/24`.
- Kali: `eth0=172.17.25.113/24`.
- Cliente: `ens34=172.17.25.114/24`.

Antes del dataset final deben desconectarse en ESXi las NIC externas de Servidor, Kali y Cliente. La del Sensor puede conservarse temporalmente para administración y actualizaciones, siempre que no forme parte de los flujos experimentales.

## Decisión G0

Estado: **NO APTO todavía**. Hora, NTP, rutas y SSH están validados; falta aislar las tres NIC externas y repetir la comprobación de rutas y conectividad.

## Segunda comprobación

Una nueva auditoría confirmó que las tres NIC permanecen `UP`. Kali y Cliente todavía poseen una ruta por defecto mediante `172.17.25.121`; Servidor conserva dos rutas por defecto. Por tanto, F0 no se inicia todavía.

En ESXi, con las VMs encendidas, abrir **Editar configuración** y desmarcar **Conectado** en el adaptador asociado con estas MAC/IP:

| VM | Interfaz del sistema | IP que debe desaparecer |
|---|---|---|
| Servidor | `ens34` | `172.17.25.112/24` |
| Kali | `eth0` | `172.17.25.113/24` |
| Cliente | `ens34` | `172.17.25.114/24` |

No desconectar las interfaces `10.10.10.x` de administración ni las interfaces LAN/DMZ. Después se repetirá `ip -br address`, `ip route`, conectividad y NTP.
