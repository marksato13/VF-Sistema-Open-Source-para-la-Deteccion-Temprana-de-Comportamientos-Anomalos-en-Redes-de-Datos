# Revisión Claude — TCP-200M/R04

Fecha: 6 de agosto de 2026. Dictamen final: **ACEPTAR CON LIMITACIONES**.

Claude autorizó previamente una única ejecución del techo `TCP-200M/R04`, sin scoring. Su resumen habló de ocho gates aunque enumeró los diez del log; Codex corrigió el conteo documental y confirmó el SHA-256 del preflight.

La primera revisión posterior se abstuvo porque el sandbox no podía listar `/srv/ppi-evidence`. Al recibir diez rutas exactas, Claude leyó manifest, deltas, resumen y stop PCAP, iperf3, EVE, telemetría, extracción, CSV y ledger. Confirmó sin discrepancias bytes, retransmisión, PCAP/drops, paquetes pesados, delta +2, EVE, filas y estado `completed`.

Claude mantiene sin causa atribuida la retransmisión y el delta Suricata +2. Codex verificó aparte conexiones, hashes, recursos, comparación R01–R03 y auditor global 112/145. Claude autoriza exclusivamente el preflight independiente `F1N-UDP-10M-R04`; no su captura ni scoring.
