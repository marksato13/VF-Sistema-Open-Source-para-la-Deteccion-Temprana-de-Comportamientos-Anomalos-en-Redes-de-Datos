# Revisión Claude — UDP-10M R02

Fecha: 28 de julio de 2026. Claude Code, modelo Haiku.

Claude emitió **ACEPTADA CON LIMITACIONES** y autorizó únicamente el preflight de `UDP-25M/R02`. Reconoció igualdad de bytes/datagramas, cero pérdida, cero reordenamiento, cero drops, captura íntegra, tráfico pesado y ventanas correlacionadas.

Se conservaron:

- 25,002,616 bytes y 17,267 datagramas iguales por extremo;
- 10.000233/10.000011 Mbit/s y jitter receptor de 0.049850 ms;
- cero pérdida y cero datagramas fuera de orden;
- 17,296 paquetes capturados, recibidos y parseados;
- 17,267 paquetes legítimos de 500–1500 bytes, 99.8323 %;
- doce `stats`, sin alerta ni semántica L7;
- tres filas distintas pero correlacionadas;
- coincidencia exacta de la ventana interior R01↔R02 dentro de `train`;
- aceptación del ensamblador y cero duplicados entre particiones.

Se corrigieron o descartaron:

- no existe tolerancia contractual de duración `±3 %`;
- `17,267 × 1,448 = 25,002,616`, no 24,990,816;
- frente a 25,000,000 bytes nominales hay +2,616, no un déficit de 10 KB;
- los dos UDP de cuatro bytes son inicialización observada; no se probaron como keepalive;
- no hay evidencia de un `seed` compartido;
- EVE no está en silencio: contiene doce eventos `stats`;
- las 14 features no son coeficientes de regresión y el modelo previsto es Isolation Forest;
- dos mediciones de CPU no forman una tendencia monotónica;
- se descartan umbrales inventados de 4/5 % CPU, 1 GiB RSS, 0.08 ms jitter y cinco `ifdrops`;
- no existe `latencia_pipeline.txt` en el contrato de campaña;
- R03 no corresponde al siguiente perfil R02;
- una divergencia de 1 % no posee gate formal y la referencia a “fase lunar” es impropia;
- UDP-25M no debe producir los mismos bytes que UDP-10M;
- la autorización no proyecta recursos, jitter, pérdida ni coincidencias del perfil siguiente.

La autorización se limita al preflight independiente de `UDP-25M/R02`; su captura dependerá de sus gates reales.

**Dictamen consolidado: ACEPTADA CON LIMITACIONES.** Estado global 55/145; R02 26/29.
