# Debilidades y mejoras — análisis punto por punto

- **Fecha:** 2026-08-18
- **Estado:** filas #1, #4, #8, #9, #10 y #12 implementadas/corregidas y desplegadas en VM02, cada una con prueba real en producción; la #9 (falso positivo) y la #10 (replay de backlog) se encontraron durante la validación en vivo del heurístico de la #1, y la #12 (atraso bajo carga) se midió en F6 y se corrigió después con parseo incremental. La fila #5 (validación final F6) se **ejecutó** y produjo las limitaciones #11 (FPR operativo sobre tráfico pesado, la debilidad más importante para el jurado — sigue abierta) y #12 (ya corregida). Las filas #2, #3, #6, #7 y #11 siguen siendo análisis/trabajo futuro.

## Propósito

Este documento recolecta, en un solo lugar, las debilidades **medidas** del sistema completo (dataset, modelo, motor, enforcement, dashboard) y qué se podría hacer con cada una — separando claramente evidencia de recomendación, y costo/riesgo de cada mejora, siguiendo el mismo criterio de rigor que el resto del proyecto: nada se afirma sin la evidencia real que lo respalda.

No es una lista de "cosas por hacer" genérica: cada fila cita el documento donde la debilidad fue medida.

## Tabla completa

| # | Debilidad medida | Evidencia | Mejora posible | Costo/riesgo |
|---|---|---|---|---|
| 1 | ✅ Detección débil en fuerza bruta / password-spray | `ANOM-AUTH-FAIL-50` 50% (3/6), `ANOM-KALI-PASSWORD-SPRAY-50` 55% (16/29) — el peor punto de los 7 modelos comparados. `docs/fase04-modelado/06-modelo-final-congelado-ocsvm.md` | **Implementado (2026-08-18):** heurístico complementario en `scripts/engine/motor_decision.py` (`auth_failure_heuristic`) — si el modelo dice PERMIT pero la ventana tiene ≥5 peticiones HTTP en 60s con ≥80% de estados 401/403, se escala a ALERT. Umbrales por criterio razonado (mismo patrón que el heurístico SSH del MVP anterior), **no** calibrados estadísticamente como el umbral del modelo — declarado así explícitamente en el código | **Bajo** — mismo patrón ya validado en producción (heurístico de ventana vacía), solo una regla nueva. Desplegado y verificado activo en VM02 |
| 2 | Un solo umbral (ALERT/PERMIT), sin nivel intermedio | Decisión de diseño documentada explícitamente, no una limitación técnica del modelo. `docs/fase05-motor-tiempo-real/01-diseno-motor-tiempo-real.md` | Calibrar un segundo umbral (tipo `LIMIT`) con el mismo método de cuantil ya usado (`alpha` distinto, sobre los mismos scores de `validation`) | **Medio** — exige repetir el proceso riguroso de calibración ya usado, nunca inventar el número directamente |
| 3 | `tls_handshake_failure_ratio_60s` constante en todo el dataset | `docs/fase03-dataset/175-limite-tls-handshake-failure-ratio.md` | Diseñar un escenario que induzca fallos reales de handshake TLS (certificado inválido a propósito, truncar la conexión) | **Medio-alto** — requiere nueva campaña de calibración y una posible ampliación v2.1 del dataset |
| 4 | ✅ Buffer en anillo del motor (~120s) más corto que una campaña offline completa | `docs/fase05-motor-tiempo-real/01-diseno-motor-tiempo-real.md` | **Implementado (2026-08-18):** `ppi-motor-capture.service` ampliado de `-W 8` (120s) a `-W 16` (~240s); `ppi-motor.service` ajustado a `--history-seconds 230` para mantener el lookback de EVE coherente con el nuevo anillo de PCAP | **Bajo** — cambio de parámetro. Costo aceptado y declarado: el motor reparsea todo el anillo cada ciclo, así que esto duplica aproximadamente el trabajo de parseo por ciclo. Desplegado y verificado activo en VM02 (proceso real con `-W 16` confirmado) |
| 5 | ✅ FPR y desempeño reales del sistema completo (motor + enforcement) no medidos en producción | Era el pendiente de validación final F6 | **Ejecutado (2026-08-18):** 2 pases de 29 corridas + 2 pruebas de aislamiento. `docs/fase07-validacion-final/02-resultados-f6.md`. Resultado: lead-time ~8s, disponibilidad 100%, heurístico validado en producción; y **dos limitaciones nuevas medidas** (filas #11 y #12) | **N/A** — validación, no una mejora; produjo hallazgos #11 y #12 |
| 6 | Enforcement es solo por IP — vulnerable a rotación de IP del atacante | Limitación estructural conocida de cualquier bloqueo por IP; no medida específicamente en este proyecto | No hay mitigación de bajo costo real dentro del alcance de esta tesis | **N/A** — se declara como límite del diseño, no se intenta resolver |
| 7 | Sin monitoreo de deriva del modelo (¿sigue siendo válido el umbral con el tiempo?) | No hay proceso definido todavía en ningún documento | Documentar (no implementar) un procedimiento de re-evaluación periódica como trabajo futuro explícito | **Bajo** costo de documentar; implementarlo queda fuera de alcance |
| 8 | ✅ Dashboard sin tendencia histórica más allá de la última hora visible | `docs/fase06-dashboard/01-diseno-dashboard-motor.md` (rediseño original, solo 60 min) | **Implementado (2026-08-18, sección A del rediseño):** selector 1h/24h en `/api/activity`, respaldado por agregación horaria sobre el mismo `motor_decision.log` — sin persistencia nueva. Verificado en VM02: `/api/activity?range=24h` devuelve 25 buckets reales. Límite que sigue vigente: no hay historial más allá de lo que retiene el propio log (sin rotación/persistencia de agregados diarios) | **Bajo** — ya desplegado. Ampliar más allá de 24h sí exigiría persistencia nueva, fuera de este alcance |
| 9 | ✅ Falso positivo: bloqueo de cliente legítimo por ventana `packet_count_10s==0` con señal L7 residual (desincronización PCAP/EVE) | Reproducido en producción: un `GET /→200` benigno de `10.20.0.20` provocó `ALERT score=0.0 pkts10=0 enforced=true`. Detalle: `docs/fase05-motor-tiempo-real/02-fp-ventana-sin-paquetes.md` (`MOTOR-FP-01`) | **Corregido (2026-08-18):** el modelo ya no puntúa ventanas sin paquetes (fuera-de-distribución); el heurístico de fuerza bruta se movió fuera del `else` del modelo para seguir disparando sobre L7. Verificado en vivo: GET benigno → `PERMIT no_live_packets_heuristic` sin bloqueo; fuerza bruta 10×401 → `ALERT auth_failure_heuristic` con bloqueo (Kali cortada a mitad del ataque) | **Bajo** — sin tocar modelo ni umbral; prueba positiva + negativa + no-regresión reales en VM02 |
| 10 | ✅ Replay de backlog al reiniciar el motor si el anillo conserva PCAP rancios | Reproducido esta sesión (`MOTOR-OBS-02` en `docs/fase05-motor-tiempo-real/02-fp-ventana-sin-paquetes.md`): tras reiniciar el motor con PCAP rancios, el log creció miles de líneas con `window_end` de ~10 h atrás y re-bloqueó IPs sobre datos viejos | **Corregido (2026-08-18):** `list_closed_pcap_files()` filtra los PCAP por `mtime`, descartando los que superan `history_seconds` (230 s). Verificado en VM02: inyectado un PCAP con `mtime` 3 h atrás + reinicio → el log creció 1 línea, sin backlog | **Bajo** — cambio acotado, con test unitario + prueba real. Queda como higiene futura (no de comportamiento) borrar PCAP rancios de disco al arrancar la captura |
| 11 | 🔴 FPR operativo sobre tráfico legítimo **pesado** — el modelo lo puntúa en el margen del umbral y algunas ventanas caen debajo → FP → bloqueo de cliente legítimo | Medido en F6: `iperf-tcp 200M` en **aislamiento** dio 1 ventana ALERT (score 1.689 < 1.8126) y bloqueó al cliente; scores del tráfico pesado apiñados en 1.69–1.99. En campaña back-to-back, 22–26% de ventanas benignas. `docs/fase07-validacion-final/02-resultados-f6.md` | **No implementado.** Recalibrar el umbral incluyendo tráfico pesado operativo en la calibración, o revisar features de volumen — requiere una calibración/evaluación formal nueva (`PM-multilayer-v2-v2`), no un ajuste directo del número | **Alto valor, medio-alto costo** — es la debilidad más importante para el jurado; exige repetir el proceso riguroso de calibración, nunca tocar el umbral a mano |
| 12 | ✅ Atraso del motor bajo carga sostenida (hasta 161 s), por reparsear el anillo de PCAP completo cada ciclo | Medido en F6: `logged_at − window_end` creció hasta 161 s bajo tráfico pesado. `docs/fase07-validacion-final/02-resultados-f6.md` | **Corregido (2026-08-19):** parseo INCREMENTAL en `scripts/engine/motor_decision.py` — cada PCAP se decodifica una sola vez a un buffer de paquetes; la atribución de flujo se recalcula sobre el buffer en memoria (`attribute_packets`), en vez de re-decodificar todo el anillo cada ciclo. Poda del buffer por tiempo-del-dato (no del reloj) para no descartar datos aún no procesados cuando va atrasado. Test de equivalencia byte a byte (553 241 obs sobre PCAP reales) + prueba en VM02: bajo descarga de 500 MB el atraso se mantuvo en 7–15 s (antes crecía sin límite), CPU ~1 %, detección de ataques intacta | **Ya implementado** — costo/ciclo bajó de ~13 s a ~8 s para descargas pesadas; queda como límite el caso extremo (iperf 200 Mbit/s concurrente sostenido), donde `attribute_packets` sobre millones de paquetes/ventana aún puede exceder el ciclo, y el sistema se recupera al cesar la carga |

## Qué NO se va a hacer sin evidencia nueva

- No se calibra un segundo umbral (`LIMIT`) inventando el número — requiere repetir el proceso de calibración con datos de `validation`, igual que el umbral actual.
- No se afirma que el enforcement por IP sea "suficiente" ni se intenta parchear con una solución improvisada (fingerprinting, MAC, etc.) sin evaluarla con el mismo rigor que el resto del sistema.
- No se reentrenan modelo ni umbral fuera de una versión nueva formal (`PM-multilayer-v2-v2`), consistente con la regla ya establecida en `docs/fase04-modelado/06-modelo-final-congelado-ocsvm.md`.

## Priorización recomendada

Las filas #1 y #4 ya se implementaron y desplegaron el 2026-08-18, por ser las de menor costo/riesgo y reutilizar patrones ya validados en producción. Las filas #2, #3 y #7 son trabajo futuro legítimo pero exigen su propio proceso de calibración/evaluación, no deben apurarse. La fila #5 (validación final) ya está planificada y es el siguiente paso natural. La fila #6 es una limitación a declarar en el informe final, no una tarea pendiente.
