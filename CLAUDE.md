# Contexto de trabajo para Claude Code

## Propósito

Este repositorio documenta y construye la versión final de un sistema open source para la detección temprana de comportamientos anómalos en redes de datos. Es un proyecto ingenieril desplegado en un laboratorio virtualizado sobre VMware ESXi.

Repositorio oficial:

```text
https://github.com/marksato13/VF-Sistema-Open-Source-para-la-Deteccion-Temprana-de-Comportamientos-Anomalos-en-Redes-de-Datos.git
```

Remoto esperado: `origin`. Rama estable: `main`.

Claude debe trabajar como revisor técnico adversarial y segundo ingeniero. Codex actúa principalmente como implementador y operador del laboratorio. El objetivo no es que ambos produzcan respuestas similares, sino utilizar revisión cruzada para encontrar errores antes de las pruebas y la defensa ante el jurado.

## Lectura obligatoria antes de opinar o modificar

Al comenzar una sesión:

1. Ejecutar `git status --short --branch` y no sobrescribir cambios ajenos.
2. Leer `docs/01-infraestructura-virtual/README.md`.
3. Leer `docs/02-mejoras-observadas-por-el-jurado/README.md`.
4. Leer `docs/03-procedimiento-ansible/README.md`.
5. Leer `ansible/README.md`, el inventario y los playbooks relacionados con la tarea.
6. Distinguir entre diseño planificado, configuración aplicada y evidencia validada.
7. Ejecutar `git remote -v` y confirmar que `origin` apunta al repositorio oficial antes de publicar.

No asumir que una tarea está completada solo porque está descrita. Buscar la evidencia registrada y, cuando exista acceso autorizado, proponer una prueba reproducible.

## Arquitectura actual

| VM | Función | PPI-MGMT | PPI-LAN | PPI-DMZ |
|---|---|---|---|---|
| VM01 | Administración, Ansible, Codex y Claude Code | `10.10.10.10` | — | — |
| VM02 | Sensor, router, Suricata y motor ML | `10.10.10.20` | `10.20.0.1` | `10.30.0.1` |
| VM03 | Servidor protegido | `10.10.10.30` | — | `10.30.0.10` |
| VM04 | Kali, generación controlada de ataques | `10.10.10.40` | `10.20.0.100` | — |
| VM05 | Cliente legítimo y tráfico pesado | `10.10.10.50` | `10.20.0.20` | — |

Flujo experimental:

```text
Cliente 10.20.0.20 ─┐
                     ├─► Sensor 10.20.0.1 / 10.30.0.1 ─► Servidor 10.30.0.10
Kali 10.20.0.100 ───┘
```

El Sensor tiene `ip_forward=1` y una política de reenvío con nftables. Cliente y Kali poseen una ruta persistente a `10.30.0.0/24` mediante `10.20.0.1`. El Servidor posee una ruta explícita de retorno a `10.20.0.0/24` mediante `10.30.0.1`.

Las NIC externas de `172.17.25.0/24` todavía existen para instalación y recuperación. El bypass ya no es hipotético: VM01 alcanzó TCP/22 del Servidor por `172.17.25.112` sin cruzar el Sensor. Todas las NIC externas de VM02–VM05 deben permanecer desconectadas en ESXi durante campañas oficiales; VM01 conserva Internet/RustDesk y administra el laboratorio por PPI-MGMT.

## Estado validado

- Acceso SSH mediante la cuenta técnica `useransible` y claves Ed25519.
- Conectividad administrativa con las cuatro VMs.
- Sensor: 6 vCPU, 16 GiB de RAM y disco virtual de 160 GiB; raíz ampliada a aproximadamente 154 GiB.
- Servidor: 2 vCPU, 4 GiB de RAM y disco virtual de 120 GiB; raíz ampliada a aproximadamente 115 GiB.
- Kali: 4 vCPU, aproximadamente 6 GiB de RAM y disco de 60 GiB.
- Cliente: 4 vCPU, 8 GiB de RAM y disco de 100 GiB; raíz ampliada a aproximadamente 98 GiB.
- Todas las VMs usan `America/Lima` y NTP sincronizado.
- El Sensor `10.10.10.20` sirve como referencia NTP interna.
- Enrutamiento Cliente/Kali hacia Servidor probado mediante ICMP, TCP/22, traza y contadores de nftables.
- Suricata 8.0.3 está instalado como IDS AF_PACKET sobre `ens35`, con `HOME_NET=[10.30.0.0/24,10.20.0.20/32]`, Emerging Threats Open, EVE JSON y una regla local de validación.
- La captura se validó con alertas ICMP y un evento HTTP completo. Sensor y Servidor superaron reinicios controlados con configuración persistente; todavía faltan ataques reales, protocolos restantes y pruebas de carga.
- La calibración segura fijó techos de F1 en 200 Mbit/s TCP, 50 Mbit/s UDP y 20 MB/s por transferencia HTTP/HTTPS; todas las pruebas acotadas posteriores registraron cero drops de Suricata.
- Existe un orquestador reproducible en `scripts/campaign/`: manifiesto, inventario, contadores, serie temporal del Sensor, segmento EVE y hashes por campaña. Los artefactos runtime permanecen fuera de Git.
- En VM02–VM05, `useransible` puede ejecutar únicamente el reinicio exacto `/usr/bin/systemctl reboot --no-wall`; en el Sensor también puede usar los helpers versionados `ppi-suricata-metrics` y `ppi-pcap-control`. No dispone de sudo general ni de permiso directo sobre `tcpdump`; la prueba negativa con `/usr/bin/id` falla en las cuatro VMs.
- G4 incorpora PCAP por campaña mediante un helper raíz que fija `ens35`, filtro LAN↔DMZ, snaplen completo y rotación máxima aproximada de 2.048 GB. El diseño y riesgos están en `docs/05-plan-pruebas/11-diseno-captura-PCAP-G4.md`.
- G4 pasó con DNS y HTTP. En `CAL-G4-HTTP-001`, 7,242 de 8,484 paquetes IPv4 (85.36 %) midieron 500–1500 bytes, con cero drops y SHA remoto/local verificado. Resultados: `docs/05-plan-pruebas/12-validacion-captura-PCAP-G4.md`.
- G5 define `multilayer-v1`: 14 features causales por IP iniciadora, con ventanas de 10/30/60 s y tres señales L7 pasivas. Diccionario: `docs/06-features-modelado/01-diccionario-multicapa-G5.md`; extractor: `scripts/features/`.
- G5 pasó pruebas sintéticas, regresión HTTP y una campaña DNS con warm-up de 60 s. La fila resultó `eligible_training=true`, pero conserva propósito `calibration` y no entra al dataset. Evidencia: `docs/06-features-modelado/02-validacion-extractor-G5.md`.
- G6 usa `f1-normal-v2`: 29 perfiles, cinco repeticiones y partición fija R01–R03/R04/R05. `v1` queda preservada para cuatro pilotos DNS/HTTP/RST/TLS. `v2` añadió HTTP hacia las VIP DMZ `.10/.11/.12`; persistieron tras reinicio y el piloto obtuvo `unique_dst_ip_ratio_30s=1.0` con cero drops. Son diversidad lógica en una sola VM, no tres hosts físicos. El gate de disco falló en ese piloto histórico y luego se resolvió con el volumen dedicado descrito abajo. Evidencia: `docs/07-dataset-campanas/05-validacion-diversidad-L3-v2.md`.
- El ensamblador `scripts/dataset/build_f1_dataset.py` exige las 145 campañas `v2`, verifica bundles, recalcula matriz/esquema desde el commit, rechaza calibraciones y reconstruye el split por repetición. En el volumen oficial reporta 15 aceptadas (`DNS-MIXED-20-2/R01`, los ocho tamaños HTTP/HTTPS R01, `HTTP-404-5/R01`, `TLS-SESSIONS-20/R01`, `HTTP-MULTI-1/R01`, `HTTP-MULTI-5/R01`, `HTTP-C2/R01` y `HTTP-C4/R01`), 0 inválidas, 0 advertencias y 130 faltantes; los cinco pilotos históricos permanecen excluidos en la raíz heredada. Revisión inicial: `docs/04-revisiones-claude/2026-07-21-ensamblador-F1.md`.
- El gate G3 del orquestador pasó con `CAL-F1-DNS-003`: 6 paquetes, cero drops/errores/overflow, 7 registros EVE exactos y 7 muestras del Sensor. Esta ejecución es calibración y no pertenece al dataset.
- VM01 conserva su disco raíz de 70 GiB y ya posee un segundo VMDK thin de 150 GiB: ext4 por UUID en `/srv/ppi-evidence`, aproximadamente 140 GiB disponibles. El montaje persistió tras reiniciar VM01 con el mismo UUID y opciones `rw,nosuid,nodev,noexec,noatime`; RustDesk volvió `active/enabled`. Los gates de capacidad e identidad de F1 están en PASS. Diseño y evidencia: `docs/08-almacenamiento/01-disco-evidencias-vm01.md`.
- La auditoría inicial G7 confirmó el bypass: después del reinicio, VM03 recuperó `ens34=172.17.25.112` y VM01 alcanzó SSH directamente sin cruzar la captura `ens35` del Sensor. Ese estado histórico **NO APTO** está en `docs/05-plan-pruebas/13-auditoria-preexperimental-G7.md`; revisión inicial: `docs/04-revisiones-claude/2026-07-22-gate-preexperimental-G7.md`.
- G7 está **APTO PERSISTENTE**: VM03 reinició con un `boot_id` nuevo, `ens34` siguió `DOWN/NO-CARRIER`, `.112` no reapareció y continuó bloqueada desde VM01. Persistieron PPI-MGMT, retorno DMZ, servicios, NTP, VIP, restricciones y el camino Cliente/Kali→Sensor→Servidor; Suricata terminó con cero drops/errores. Se autoriza avanzar canario por canario, con preflight y auditoría entre ejecuciones; no el lote completo. Evidencia: `docs/05-plan-pruebas/15-validacion-persistencia-G7.md`; revisión condicional previa: `docs/04-revisiones-claude/2026-07-22-cierre-operacional-G7.md`.
- El primer canario oficial `F1N-DNS-MIXED-20-2-R01` fue aceptado: 44/44 paquetes, 20 NOERROR + 2 NXDOMAIN, delta Suricata 48, 54 EVE exactos, cero drops/errores, una fila elegible y `dns_nxdomain_ratio_60s=2/22`. Evidencia: `docs/07-dataset-campanas/06-primer-canario-oficial-F1.md`.
- El segundo canario `F1N-HTTP-10MB-R01` también fue aceptado: descarga HTTP 200 de 10,485,760 bytes, PCAP de 11,142,194 bytes, 7,912/7,912 paquetes y cero drops. 7,248 paquetes (91.6077 %) midieron 500–1500 bytes y la fila benigna obtuvo `large_ip_ratio_10s=0.91607685`. EVE limitó la inspección del archivo a 102,400 bytes (`fileinfo.state=TRUNCATED`), pero la descarga y el PCAP no fueron truncados; no afirmar inspección completa de contenido. El ensamblador suma 2 aceptadas y 143 faltantes. Evidencia: `docs/07-dataset-campanas/07-canario-HTTP-10MB-F1.md`.
- El tercer canario `F1N-HTTP-100MB-R01` fue aceptado: HTTP 200 de 104,857,600 bytes, PCAP de 111,438,325 bytes, 79,114/79,114 paquetes, 91.6172 % entre 500–1500 bytes, dos ventanas elegibles y cero drops. Suricata llegó a 21.62 % CPU y 776,248 KiB RSS sin presión. El límite `fileinfo=102400/TRUNCATED` se mantiene: no afirmar inspección completa del cuerpo. El ensamblador suma 3 aceptadas y 142 faltantes. Claude/Haiku emitió ACEPTAR, con una palabra “calibración” corregida contra manifiesto y ledger. Evidencia: `docs/07-dataset-campanas/08-canario-HTTP-100MB-F1.md` y `docs/04-revisiones-claude/2026-07-22-canario-HTTP-100MB-F1.md`.
- El cuarto canario `F1N-HTTP-500MB-R01` fue aceptado: 524,288,000 bytes HTTP 200, dos PCAP íntegros de 554,956,808 bytes, 368,467/368,467 paquetes, 98.3499 % en 500–1500 bytes, cuatro filas elegibles y cero drops. CPU máxima 26.20 %, RSS 776,372 KiB. Los 6,080 paquetes pequeños son todos TCP, 6,075 sin payload y cero fragmentados. El ensamblador suma 4 aceptadas y 141 faltantes. Claude/Haiku emitió ACEPTAR con observaciones, cerradas contra CSV y PCAP. Evidencia: `docs/07-dataset-campanas/09-canario-HTTP-500MB-F1.md` y `docs/04-revisiones-claude/2026-07-22-canario-HTTP-500MB-F1.md`.
- El quinto canario `F1N-HTTP-1GB-R01` fue aceptado con no conformidad cerrada: 1,073,741,824 bytes HTTP 200, tres PCAP íntegros de 1,136,327,873 bytes, 751,835/751,835 paquetes, 98.7155 % en 500–1500 bytes, seis filas elegibles y cero drops. Un flow `/health` previo fue emitido dentro de EVE por timeout, pero empezó antes, no está en PCAP, el extractor ignora flows y todas las ventanas empiezan después de la campaña. Claude/Haiku condicionó la aceptación a esa prueba, que pasó. Desde el commit posterior, el runner aplica 70 s de quietud previa oficial sin cambiar matriz/esquema. Ensamblador: 5 aceptadas, 140 faltantes. Evidencia: `docs/07-dataset-campanas/10-canario-HTTP-1GB-F1.md` y `docs/04-revisiones-claude/2026-07-22-canario-HTTP-1GB-F1.md`.
- El sexto canario `F1N-HTTPS-10MB-R01` fue aceptado con limitaciones: 10,485,760 bytes HTTPS 200 confirmados por Cliente, PCAP íntegro de 11,130,372 bytes, 7,608/7,608 paquetes, 95.4127 % en 500–1500 bytes, dos filas elegibles, `tls_session_rate_60s=1/60` y cero drops. EVE observó TLS 1.3 con JA3/JA3S/JA4, pero no HTTP/fileinfo cifrado. El certificado es autofirmado y `curl` usa `--insecure`; no representa PKI productiva. Un flow IPv6 link-local nacido durante la quietud quedó en EVE crudo pero fuera de PCAP/features. Claude/Haiku emitió ACEPTAR CONDICIONADO y las condiciones se documentaron. Ensamblador: 6 aceptadas, 139 faltantes. Evidencia: `docs/07-dataset-campanas/11-canario-HTTPS-10MB-F1.md` y `docs/04-revisiones-claude/2026-07-22-canario-HTTPS-10MB-F1.md`.
- El séptimo canario `F1N-HTTPS-100MB-R01` fue aceptado con limitaciones: 104,857,600 bytes HTTPS 200, PCAP íntegro de 111,210,058 bytes, 74,858/74,858 paquetes, 96.9502 % en 500–1500 bytes, dos filas elegibles, una sesión TLS 1.3 y cero drops. Dos flows mDNS nacidos durante la quietud quedaron fuera del PCAP/features. Claude/Haiku autorizó HTTPS 500 MB condicionado a integridad; se corrigió su mención de fileinfo porque HTTPS no produjo uno y se conserva `tls_session_rate_60s` como L7 pasiva. Ensamblador: 7 aceptadas, 138 faltantes. Evidencia: `docs/07-dataset-campanas/12-canario-HTTPS-100MB-F1.md` y `docs/04-revisiones-claude/2026-07-22-canario-HTTPS-100MB-F1.md`.
- El octavo canario `F1N-HTTPS-500MB-R01` fue aceptado con limitaciones: 524,288,000 bytes HTTPS 200, dos PCAP íntegros de 555,929,941 bytes, 371,438/371,438 paquetes, 97.7033 % en 500–1500 bytes, tres filas elegibles, TLS 1.3 y cero drops. EVE quedó limpio: 16 stats y un TLS. Claude/Haiku pidió concurrencia para HTTPS 1 GB; se mantiene el contrato de una sola transferencia y la diversidad se medirá separadamente con `TLS-SESSIONS-20`. Ensamblador: 8 aceptadas, 137 faltantes. Evidencia: `docs/07-dataset-campanas/13-canario-HTTPS-500MB-F1.md` y `docs/04-revisiones-claude/2026-07-22-canario-HTTPS-500MB-F1.md`.
- El noveno canario `F1N-HTTPS-1GB-R01` fue aceptado: 1,073,741,824 bytes HTTPS 200, tres PCAP íntegros de 1,138,215,605 bytes, 757,999/757,999 paquetes, 98.0435 % en 500–1500 bytes, siete filas elegibles, TLS 1.3 y cero drops. La séptima fila contiene cuatro FIN/ACK y se conserva como cola normal de baja actividad; no hay duplicados. Claude/Haiku emitió ACEPTAR y confirmó que `HTTP-404-5/R01` sigue en orden antes de `TLS-SESSIONS-20/R01`. Ensamblador: 9 aceptadas, 136 faltantes. Evidencia: `docs/07-dataset-campanas/14-canario-HTTPS-1GB-F1.md` y `docs/04-revisiones-claude/2026-07-22-canario-HTTPS-1GB-F1.md`.
- El décimo canario `F1N-HTTP-404-5-R01` fue aceptado con límites declarados: cinco GET secuenciales produjeron cinco HTTP 404 y cinco `fileinfo CLOSED`; el PCAP íntegro contiene 50/50 paquetes, 5 SYN, 5 SYN/ACK, 10 FIN, 0 RST y cero drops. Las dos filas elegibles tienen `http_error_ratio_60s=1`; comparten historia y no representan muestras independientes. El 0 % de paquetes pesados es correcto para este estrato pequeño. Kali conservaba `.113` configurada, pero su NIC externa estaba `DOWN`, sin ruta y bloqueada desde VM01; debe mantenerse como gate. Claude/Haiku emitió ACEPTAR. Ensamblador: 10 aceptadas, 135 faltantes. Siguiente perfil: `TLS-SESSIONS-20/R01`, veinte sesiones secuenciales. Evidencia: `docs/07-dataset-campanas/15-canario-HTTP-404-5-F1.md` y `docs/04-revisiones-claude/2026-07-22-canario-HTTP-404-5-F1.md`.
- El undécimo canario `F1N-TLS-SESSIONS-20-R01` fue aceptado con limitaciones: veinte conexiones HTTPS secuenciales en 2.46 s produjeron veinte HTTP 200 en Cliente, veinte eventos TLS 1.3 y 431/431 paquetes; 20 SYN, 20 SYN/ACK, 40 FIN, 0 RST y cero drops. Las dos filas elegibles alcanzan `tls_session_rate_60s=0.25` y `0.33333333`, pero comparten el episodio. Todas las sesiones usan un cliente, destino y huella JA3/JA3S/JA4; certificado autofirmado y `--insecure`. Claude/Haiku emitió ACEPTAR CONDICIONADO y sus condiciones quedaron declaradas. Ensamblador: 11 aceptadas, 134 faltantes. Siguiente: `HTTP-MULTI-1/R01`, secuencial y multidestino; la concurrencia pertenece a C2/C4/C8. Evidencia: `docs/07-dataset-campanas/16-canario-TLS-SESSIONS-20-F1.md` y `docs/04-revisiones-claude/2026-07-22-canario-TLS-SESSIONS-20-F1.md`.
- El duodécimo canario `F1N-HTTP-MULTI-1-R01` fue aceptado con limitaciones: tres GET `/health` secuenciales alcanzaron `.10/.11/.12`, produjeron tres HTTP 200, tres `fileinfo CLOSED` y 30/30 paquetes; 3 SYN, 3 SYN/ACK, 6 FIN, 0 RST y cero drops. La fila elegible obtuvo `unique_dst_ip_ratio_30s=3/3=1`, `unique_dst_port_ratio_30s=1/3` y completitud SYN 1.0. Las VIP son identidades lógicas en una sola VM, no hosts físicos. Claude/Haiku emitió ACEPTAR CONDICIONADO; se corrigió que la fila es oficial `experiment/train` y que ratio 1.0 no es exclusivo. Ensamblador: 12 aceptadas, 133 faltantes. Siguiente: `HTTP-MULTI-5/R01`, esperado 3/15=0.2. Evidencia: `docs/07-dataset-campanas/17-canario-HTTP-MULTI-1-F1.md` y `docs/04-revisiones-claude/2026-07-22-canario-HTTP-MULTI-1-F1.md`.
- El decimotercer canario `F1N-HTTP-MULTI-5-R01` fue aceptado con limitaciones: quince GET `/health` secuenciales —cinco por VIP— produjeron quince HTTP 200, quince `fileinfo CLOSED` y 150/150 paquetes; 15 SYN, 15 SYN/ACK, 30 FIN, 0 RST y cero drops. La fila elegible obtuvo `unique_dst_ip_ratio_30s=3/15=0.2`, `unique_dst_port_ratio_30s=1/15` y completitud SYN 1.0. El episodio duró 1.67 s y las VIP siguen siendo lógicas. Claude/Haiku emitió ACEPTAR CONDICIONADO; se corrigieron conteo de flows, propósito y unidades del siguiente perfil. Ensamblador: 13 aceptadas, 132 faltantes. Siguiente: `HTTP-C2/R01`, dos flujos de 100 MB a `10M` bytes/s cada uno. Evidencia: `docs/07-dataset-campanas/18-canario-HTTP-MULTI-5-F1.md` y `docs/04-revisiones-claude/2026-07-22-canario-HTTP-MULTI-5-F1.md`.
- El decimocuarto canario `F1N-HTTP-C2-R01` fue aceptado con limitaciones: dos descargas concurrentes de 100 MB se solaparon durante 9.52 s, completaron 209,715,200 bytes y produjeron 151,467/151,467 paquetes; 95.7053 % en 500–1500 bytes, 2 SYN, 2 SYN/ACK, 4 FIN, 0 RST y cero drops. El agregado observado fue 176.15 Mbit/s frente a 167.77 nominal, aún bajo 200. Dos filas elegibles registran carga concurrente; la segunda tiene completitud SYN 0 porque no contiene SYN nuevos. `fileinfo` truncó a 102,400 bytes sin truncar descarga/PCAP. Claude/Haiku emitió ACEPTAR y autorizó C4 con gates. Ensamblador: 14 aceptadas, 131 faltantes. Evidencia: `docs/07-dataset-campanas/19-canario-HTTP-C2-F1.md` y `docs/04-revisiones-claude/2026-07-22-canario-HTTP-C2-F1.md`.
- El decimoquinto canario `F1N-HTTP-C4-R01` fue aceptado con limitaciones: cuatro descargas concurrentes de 100 MB se iniciaron en 13.772 ms, se solaparon unos 19.5 s y completaron 419,430,400 bytes. El PCAP íntegro contiene 301,517/301,517 paquetes, 96.1803 % entre 500–1500 bytes, 4 SYN, 4 SYN/ACK, 8 FIN, 0 RST y cero drops. El agregado observado fue 171.94 Mbit/s, 2.48 % sobre el nominal pero 28.06 Mbit/s bajo el techo. Dos filas representan carga y una tercera conserva trece FIN/ACK de cierre; no son repeticiones independientes. Dos mDNS quedaron fuera del PCAP/features y cuatro `fileinfo` se limitaron a 102,400 bytes sin truncar transferencias. Claude/Haiku emitió ACEPTAR CONDICIONADO y autorizó C8; se descartaron como evidencia su predicción de CPU y una progresión incorrecta de tres flujos. Ensamblador: 15 aceptadas, 130 faltantes. Evidencia: `docs/07-dataset-campanas/20-canario-HTTP-C4-F1.md` y `docs/04-revisiones-claude/2026-07-23-canario-HTTP-C4-F1.md`.
- El preflight de `HTTP-C8/R01` se detuvo sin crear artefactos porque el Sensor perdió la marca `NTPSynchronized=yes` después de unas 18 horas sin alcanzar sus fuentes públicas por la NIC externa aislada. Se aplicó la jerarquía VM01 `10.10.10.10`→Sensor→VM03–VM05. `prefer require` —sin `trust`— resolvió la espera causada por `authselectmode mix`: el Sensor seleccionó VM01, pasó a estrato 4 y recuperó `Leap status: Normal`. Tres gates consecutivos pasaron con offsets máximos inferiores a 100 ms; Kali se valida mediante `systemd-timesyncd`, no `chronyc`. Hashes desplegados y Git coinciden, las NIC externas siguen abajo y el bypass continúa bloqueado. Se autoriza repetir el preflight completo de C8. Evidencia: `docs/05-plan-pruebas/16-correccion-ntp-interno-G7.md`.
- El intento oficial `F1N-HTTP-C8-R01` fue rechazado: las ocho descargas HTTP 200 completaron 838,860,800 bytes y Suricata tuvo cero drops, pero `tcpdump` capturó 596,704 de 597,180 paquetes y descartó 476 (0.079708 %). El bundle quedó `evidence_failed`, el ledger `failed` y no existen features. Dos PCAP íntegros conservan 97.1282 % de paquetes entre 500–1500 bytes, solo como diagnóstico. Claude/Haiku confirmó el rechazo y recomendó aislar el cambio de búfer. Se versionan `-B 65536` y `net.core.rmem_max=67108864`; la rotación sigue 512 MB × 4. Próximo paso: desplegar y ejecutar `CAL-G6-HTTP-C8-R01`; el retry oficial exige cero drops y una política de archivado preservando el ID canónico. Evidencia: `docs/07-dataset-campanas/21-intento-rechazado-HTTP-C8-F1.md` y `docs/04-revisiones-claude/2026-07-23-intento-rechazado-HTTP-C8-F1.md`.
- La calibración `CAL-G6-HTTP-C8-R01` con el mismo perfil/rotación y solo el búfer ampliado fue completada: 605,266/605,266 paquetes, cero drops de tcpdump/Suricata, ocho HTTP 200, 838,860,800 bytes, 38/38 EVE, dos PCAP íntegros y seis filas. `purpose=calibration` y `partition=excluded_calibration` impiden incorporarla a F1. Claude/Haiku autorizó condicionalmente el retry después de documentar hashes, archivar de forma recuperable el fallido, obtener auditoría 15 aceptadas/0 inválidas/130 faltantes y repetir preflight. La rotación no cambia y el retry aún no se ejecutó. Evidencia: `docs/07-dataset-campanas/22-calibracion-buffer-HTTP-C8-G6.md` y `docs/04-revisiones-claude/2026-07-23-calibracion-buffer-HTTP-C8-G6.md`.
- El intento rechazado `F1N-HTTP-C8-R01` se archivó sin eliminación como `attempt-01` en VM01 y Sensor mediante el commit `3860c864`. Los hashes del manifest, ledger, lista del bundle y ambos PCAP se volvieron a verificar; las rutas activas quedaron libres. El ensamblador regresó al estado esperado: 15 aceptadas, 0 inválidas, 0 advertencias y 130 faltantes; la calibración sigue excluida. El reintento debe reutilizar el ID canónico y ejecutar nuevamente todos los gates. Evidencia: `docs/05-plan-pruebas/17-archivado-intentos-fallidos.md`.
- El reintento oficial `F1N-HTTP-C8-R01` fue aceptado con limitaciones: ocho HTTP 200 transfirieron 838,860,800 bytes; los ocho SYN comenzaron dentro de 46.726 ms y cada flujo duró aproximadamente 49.51 s. El PCAP íntegro contiene 600,128/600,128 paquetes, cero drops y 96.6514 % entre 500–1500 bytes; Suricata procesó un delta de 600,134 con cero drops/errores. Se extrajeron seis filas elegibles, autocorrelacionadas dentro de un episodio. El ensamblador suma 16 aceptadas, 0 inválidas y 129 faltantes; calibración e intento fallido permanecen separados. Claude aceptó tras corregir una mezcla inicial de generaciones y cifras históricas. Siguiente perfil: `TCP-REFUSED-5/R01`, siempre con nuevo preflight. Evidencia: `docs/07-dataset-campanas/23-canario-HTTP-C8-F1.md` y `docs/04-revisiones-claude/2026-07-23-canario-HTTP-C8-F1.md`.
- `F1N-TCP-REFUSED-5-R01` fue aceptado con limitaciones: cinco intentos legítimos al único puerto cerrado `10.30.0.10:65000` produjeron exactamente cinco SYN y cinco RST/ACK en 2.449771 s, cero drops y ninguna sesión L7. Dos filas elegibles obtuvieron `syn_completion_ratio_10s=0` y `rst_ratio_10s=0.5`; la fila final usa correctamente `unique_dst_port_ratio_30s=1/5=0.2`. Son ventanas autocorrelacionadas de un episodio. El ensamblador suma 17 aceptadas, 0 inválidas y 128 faltantes. Claude aceptó después de verificar la fórmula y corregir confusiones entre puertos origen/destino. Siguiente perfil: `TCP-50M/R01`. Evidencia: `docs/07-dataset-campanas/24-canario-TCP-REFUSED-5-F1.md` y `docs/04-revisiones-claude/2026-07-23-canario-TCP-REFUSED-5-F1.md`.
- `F1N-TCP-50M-R01` fue aceptado con limitaciones: iperf3 transfirió 125,042,688 bytes en 20 s a 50.012824/50.007704 Mbit/s, con una retransmisión y bytes iguales en ambos extremos. El PCAP contiene 90,832/90,832 paquetes, cero drops y 95.5775 % entre 500–1500 bytes. Suricata tuvo cero drops/errores pero emitió SID 2260003 y `APPLAYER_PROTO_DETECTION_SKIPPED` al no clasificar iperf3; el evento allowed se conserva, no etiqueta ataque ni entra en las 14 features. Las tres filas elegibles pertenecen a un episodio. Ensamblador: 18 aceptadas, 0 inválidas y 127 faltantes. Claude aceptó tras corregir cifras y cerrar condiciones. Siguiente: `TCP-100M/R01`. Evidencia: `docs/07-dataset-campanas/25-canario-TCP-50M-F1.md` y `docs/04-revisiones-claude/2026-07-24-canario-TCP-50M-F1.md`.
- `F1N-TCP-100M-R01` fue aceptado con limitaciones: iperf3 entregó 250,085,376 bytes en ambos extremos a 100.025523/100.022658 Mbit/s, con cuatro retransmisiones de causa no determinada. PCAP: 181,684/181,684, cero drops y 95.5681 % entre 500–1500 bytes. Suricata repitió la alerta/anomalía permitida de clasificación iperf3 sin drops ni errores; no entra en las features. Tres filas elegibles pertenecen a un episodio. Ensamblador: 19 aceptadas, 0 inválidas y 126 faltantes. Claude aceptó después de retirar porcentajes y umbrales de retransmisión inventados. Siguiente: `TCP-200M/R01`. Evidencia: `docs/07-dataset-campanas/26-canario-TCP-100M-F1.md` y `docs/04-revisiones-claude/2026-07-26-canario-TCP-100M-F1.md`.
- `F1N-TCP-200M-R01` fue aceptado con limitaciones: iperf3 entregó 500,039,680 bytes en ambos extremos a 199.998072/199.961822 Mbit/s, con cinco retransmisiones de causa no determinada. PCAP: 366,334/366,334, cero drops y 94.7641 % entre 500–1500 bytes. Suricata repitió la alerta de clasificación iperf3 sin drops/errores. Tres filas elegibles pertenecen a un episodio. Ensamblador: 20 aceptadas, 0 inválidas y 125 faltantes. Claude aceptó; su afirmación de retransmisiones “normales” se restringió a esta evidencia. Siguiente: `UDP-10M/R01`. Evidencia: `docs/07-dataset-campanas/27-canario-TCP-200M-F1.md` y `docs/04-revisiones-claude/2026-07-26-canario-TCP-200M-F1.md`.
- `F1N-UDP-10M-R01` fue aceptado con limitaciones: iperf3 envió y recibió 25,002,616 bytes en 17,267 datagramas, con 9.997181 Mbit/s en receptor, jitter de 0.027371 ms, 0 % de pérdida y cero fuera de orden. PCAP: 17,298/17,298, cero drops y 99.8208 % entre 500–1500 bytes. Suricata tuvo cero drops/errores y solo 12 eventos stats. Tres filas elegibles autocorrelacionadas tienen ratios de paquetes grandes entre 0.99312039 y 1.0, sin observaciones L7. Ensamblador: 21 aceptadas, 0 inválidas y 124 faltantes. Claude aceptó tras dos correcciones por umbrales inventados y mezcla de perfiles. Siguiente: `UDP-25M/R01`. Evidencia: `docs/07-dataset-campanas/28-canario-UDP-10M-F1.md` y `docs/04-revisiones-claude/2026-07-26-canario-UDP-10M-F1.md`.
- `F1N-UDP-25M-R01` fue aceptado con limitaciones: iperf3 envió y recibió 62,504,368 bytes en 43,166 datagramas, con 24.999210 Mbit/s en receptor, jitter de 0.068925 ms, 0 % de pérdida y cero fuera de orden. PCAP: 43,195/43,195, cero drops y 99.9329 % entre 500–1500 bytes. Suricata tuvo cero drops/errores y solo 12 stats. Tres filas elegibles autocorrelacionadas tienen ratios grandes entre 0.99841521 y 1.0, sin observaciones L7. Ensamblador: 22 aceptadas, 0 inválidas y 123 faltantes. Claude aceptó después de corregir afirmaciones no sustentadas sobre fragmentación, sesgo y causa del jitter. Siguiente: `UDP-50M/R01`. Evidencia: `docs/07-dataset-campanas/29-canario-UDP-25M-F1.md` y `docs/04-revisiones-claude/2026-07-26-canario-UDP-25M-F1.md`.
- `F1N-UDP-50M-R01` fue aceptado con limitaciones: iperf3 envió y recibió 125,007,288 bytes en 86,331 datagramas, con 49.985031 Mbit/s en receptor, jitter de 0.132158 ms, 0 % de pérdida y cero fuera de orden. PCAP: 86,364/86,364, cero drops y 99.9618 % entre 500–1500 bytes. Suricata tuvo cero drops/errores y solo 13 stats. Tres filas elegibles autocorrelacionadas tienen ratios grandes entre 0.99059406 y 1.0, sin observaciones L7. Ensamblador: 23 aceptadas, 0 inválidas y 122 faltantes. Claude aceptó después de retirar categorías no definidas sobre jitter/carga. Cierra UDP R01 10/25/50; siguiente: `MIXED-LIGHT/R01`. Evidencia: `docs/07-dataset-campanas/30-canario-UDP-50M-F1.md` y `docs/04-revisiones-claude/2026-07-27-canario-UDP-50M-F1.md`.
- `F1N-MIXED-LIGHT-R01` fue aceptado con limitaciones: HTTP 200 descargó 104,857,600 bytes; iperf3 entregó 62,521,344 bytes a ~50 Mbit/s con dos retransmisiones de causa no determinada; DNS produjo veinte pares `NOERROR`. Los tres componentes comenzaron dentro de 20.025 ms y se solaparon 0.518239 s. PCAP: 122,802/122,802, 177,537,599 bytes, cero drops y 94.3731 % entre 500–1500 bytes. EVE: 57/57; Suricata cero errores. Tres filas elegibles combinan 23 intentos, 3 SYN completos, HTTP 200 y DNS NXDOMAIN 0/20. Ensamblador: 24 aceptadas, 0 inválidas y 121 faltantes. Claude aceptó, pero tres rondas de corrección dejaron errores de unidades/contexto documentados. Quedan cinco gaps R01; siguiente: `DNS-VALID-10/R01`. Evidencia: `docs/07-dataset-campanas/31-canario-MIXED-LIGHT-F1.md` y `docs/04-revisiones-claude/2026-07-27-canario-MIXED-LIGHT-F1.md`.
- `F1N-DNS-VALID-10-R01` fue aceptado con limitaciones: diez consultas `server.ppi.lab/A` produjeron diez respuestas `10.30.0.10`, todas `NOERROR`, en 0.242729 s. PCAP: 20/20 paquetes, 2,324 bytes y cero drops; EVE: 20 DNS + 10 stats. La fila elegible registra diez intentos, un destino, un puerto y NXDOMAIN 0/10; todos los paquetes son pequeños, como corresponde al estrato. El delta Suricata fue 24 frente a 20 paquetes del PCAP por alcances de captura distintos; los cuatro adicionales no se clasifican. Ensamblador: 25 aceptadas, 0 inválidas y 120 faltantes. Claude aceptó después de corregir una confusión entre contadores. Quedan cuatro gaps R01; siguiente: `DNS-VALID-200/R01`. Evidencia: `docs/07-dataset-campanas/32-canario-DNS-VALID-10-F1.md` y `docs/04-revisiones-claude/2026-07-27-canario-DNS-VALID-10-F1.md`.
- `F1N-DNS-VALID-200-R01` fue aceptado con limitaciones: 200 consultas `server.ppi.lab/A` produjeron 200 respuestas `10.30.0.10`, todas `NOERROR`, en 4.076892 s. PCAP: 400/400 paquetes, 46,024 bytes y cero drops; EVE: 400 DNS + 10 stats + un Router Solicitation ICMPv6 fuera de alcance. El puerto `39878` se reutilizó: son 200 transacciones L7 y 199 flow attempts L3/L4. Dos filas elegibles son ventanas de la misma ráfaga, con NXDOMAIN 0/200. Ensamblador: 26 aceptadas, 0 inválidas y 119 faltantes. Claude aceptó después de corregir protocolo, conteo y unidades. Quedan tres gaps R01; siguiente: `DNS-MIXED-50-10/R01`. Evidencia: `docs/07-dataset-campanas/33-canario-DNS-VALID-200-F1.md` y `docs/04-revisiones-claude/2026-07-27-canario-DNS-VALID-200-F1.md`.
- `F1N-DNS-MIXED-50-10-R01` fue aceptado con limitaciones: 50 consultas válidas seguidas de 10 errores controlados produjeron 50 respuestas `NOERROR` y 10 `NXDOMAIN` en 1.260866 s. PCAP: 120/120 paquetes, 13,866 bytes y cero drops; EVE: 120 DNS + 10 stats. La única fila elegible registra 60 consultas e intentos, `dns_nxdomain_ratio_60s=10/60=0.16666667` y paquetes pequeños. El orden válido→NXDOMAIN no fue aleatorizado y la separabilidad contra DNS anómalo todavía no está demostrada. Ensamblador: 27 aceptadas, 0 inválidas, 0 advertencias y 118 faltantes. Claude aceptó, pero introdujo varios conteos y hechos falsos que se corrigieron contra los artefactos. Quedan dos gaps R01; siguiente: `PING-10/R01`. Evidencia: `docs/07-dataset-campanas/34-canario-DNS-MIXED-50-10-F1.md` y `docs/04-revisiones-claude/2026-07-27-canario-DNS-MIXED-50-10-F1.md`.
- `F1N-PING-10-R01` fue aceptado con limitaciones: diez echo request y diez reply, mismo ID y secuencias 1–10 produjeron 20/20 paquetes de 84 bytes, cero pérdida y cero drops. EVE contiene diez alertas permitidas SID `1000001` de telemetría y once stats; no representan ataque. La fila elegible registra un intento canónico ICMP, `icmp_ratio_10s=1`, `unique_dst_ip_ratio_30s=1/1` y ratio de puertos 0 por ausencia de transporte. El delta Suricata fue 25 frente a PCAP 20; los cinco adicionales no están identificados. Ensamblador: 28 aceptadas, 0 inválidas, 0 advertencias y 117 faltantes. Claude aceptó tras cuatro rondas de corrección, incluida contaminación con cifras DNS. Solo queda `PING-100/R01` para cerrar R01. Evidencia: `docs/07-dataset-campanas/35-canario-PING-10-F1.md` y `docs/04-revisiones-claude/2026-07-27-canario-PING-10-F1.md`.
- `F1N-PING-100-R01` fue aceptado con limitaciones y cerró R01 29/29: cien echo request y cien reply produjeron 200/200 paquetes de 84 bytes, cero pérdida y cero drops en 20.553039 s de solicitudes. Cuatro filas elegibles contienen 6/96/96/2 paquetes y son ventanas autocorrelacionadas de un episodio; todas tienen `icmp_ratio_10s=1`. EVE conserva cien alertas permitidas SID `1000001` y doce stats. Suricata contó 204 frente a PCAP 200; los cuatro adicionales no se identifican. Ensamblador: 29 aceptadas, 0 inválidas, 0 advertencias, 0 duplicados y 116 faltantes, exactamente 29 por cada R02–R05. Claude aceptó, pero una corrección se contaminó por completo con otra campaña y fue descartada. Antes de `DNS-VALID-10/R02` se realizará una auditoría agregada R01. Evidencia: `docs/07-dataset-campanas/36-canario-PING-100-cierre-R01-F1.md` y `docs/04-revisiones-claude/2026-07-27-canario-PING-100-cierre-R01-F1.md`.
- La auditoría agregada R01 pasó en `373fcba`: 29/29 perfiles, 77/77 filas elegibles, 4,382,327 observaciones de paquete, 390 de aplicación, 0 inválidas/advertencias y 14/14 features con soporte. El rango pesado aparece en 59 filas de 19 campañas, con mediana `large_ip_ratio_10s=0.95962639`. Web/TLS aporta 44/77 filas; RST, HTTP-error y NXDOMAIN son escasos y se vigilarán sin introducir ataques en F1. Solo dos ventanas de `PING-100` repiten exactamente vector dentro del mismo episodio; no hay coincidencias entre campañas ni particiones. Claude propuso umbrales, rebalanceos y ataques no sustentados; se conservaron únicamente la comparación R01↔R02 y la política explícita de coincidencias. Estado: **APTO CON CONDICIONES PARA R02**; siguiente `DNS-VALID-10/R02`. Evidencia: `docs/07-dataset-campanas/37-auditoria-agregada-R01-F1.md` y `docs/04-revisiones-claude/2026-07-27-auditoria-agregada-R01-F1.md`.
- `F1N-DNS-VALID-10-R02` fue aceptada e inició R02: diez requests y diez responses DNS `NOERROR`, 20/20 paquetes, cero drops, EVE 29/29 y una fila elegible. Su vector de 14 features coincide exactamente con R01, pero PCAP, EVE, ledger y tiempos son independientes; es repetibilidad del generador determinista, no cruce de partición. El ensamblador queda 30/145, sin inválidas ni advertencias, con una coincidencia entre campañas y cero entre particiones. El gate de campaña pasa; R02 continúa incompleta 1/29. Claude primero negó erróneamente la existencia de artefactos externos y luego corrigió a aceptar, aunque volvió a inventar una feature `request_rate` y confundió el gate agregado. Evidencia: `docs/07-dataset-campanas/38-canario-DNS-VALID-10-R02.md` y `docs/04-revisiones-claude/2026-07-27-canario-DNS-VALID-10-R02.md`. Siguiente: preflight individual de `DNS-VALID-200/R02`.
- `F1N-DNS-VALID-200-R02` fue aceptada con limitaciones: 200 requests y 200 responses `NOERROR`, PCAP 400/400, cero drops, EVE 410/410 y dos filas elegibles 24/376. Frente a R01 228/172, la diferencia se explica principalmente porque el episodio R02 empezó 0.29 s antes del borde UTC fijo, aunque ambos duraron unos 4.1 s. Las dos filas son autocorrelacionadas y el ensamblador conserva ambas; se evaluará sensibilidad por campaña antes de entrenar. Estado global 31/145, R02 2/29, cero inválidas/advertencias y ninguna coincidencia nueva. Claude aceptó, pero se corrigieron su afirmación de una sola ventana, su política inexistente de una fila por campaña y su cálculo NXDOMAIN. Evidencia: `docs/07-dataset-campanas/39-canario-DNS-VALID-200-R02.md` y `docs/04-revisiones-claude/2026-07-27-canario-DNS-VALID-200-R02.md`. Siguiente: preflight individual de `DNS-MIXED-20-2/R02`.
- `F1N-DNS-MIXED-20-2-R02` fue aceptada con limitaciones: 22 requests y 22 responses, 20 `NOERROR`, 2 NXDOMAIN, PCAP 44/44, EVE 53/53, cero drops y una fila elegible con `dns_nxdomain_ratio_60s=2/22`. El extractor cuenta 24 observaciones internas —22 queries más dos marcadores NXDOMAIN—, no 24 transacciones. El vector coincide exactamente con R01, pero los artefactos y tiempos son independientes. Estado global 32/145, R02 3/29, dos coincidencias entre campañas y cero entre particiones. Claude aceptó; se descartaron su proyección de 145 filas, su afirmación sobre deduplicación automática de Isolation Forest, un umbral arbitrario y una fase F5 inexistente. Evidencia: `docs/07-dataset-campanas/40-canario-DNS-MIXED-20-2-R02.md` y `docs/04-revisiones-claude/2026-07-27-canario-DNS-MIXED-20-2-R02.md`. Siguiente: preflight individual de `DNS-MIXED-50-10/R02`.
- `F1N-DNS-MIXED-50-10-R02` fue aceptada con limitaciones: 60 requests y 60 responses, 50 `NOERROR`, 10 NXDOMAIN, PCAP 120/120, EVE 129/129, cero drops y una fila elegible con `dns_nxdomain_ratio_60s=10/60`. Las 70 observaciones internas son 60 queries más diez marcadores NXDOMAIN. El vector coincide con R01 y añade la tercera coincidencia exacta dentro de `train`; los artefactos y tiempos son independientes. Estado global 33/145, R02 4/29, cero inválidas/advertencias y cero coincidencias entre particiones. La primera revisión Claude analizó el perfil equivocado; la corrección aceptó, pero confundió `DNS-VALID-200`, la partición y el estado del ensamblador. Evidencia: `docs/07-dataset-campanas/41-canario-DNS-MIXED-50-10-R02.md` y `docs/04-revisiones-claude/2026-07-27-canario-DNS-MIXED-50-10-R02.md`. Siguiente: preflight individual de `PING-10/R02`.
- `F1N-PING-10-R02` fue aceptada con limitaciones: diez echo request/reply, PCAP 20/20, cero pérdida/drops, EVE 21/21 con diez alertas de telemetría permitida SID `1000001`, y dos filas elegibles 18/2. R01 tuvo una fila de 20; R02 cruzó el borde UTC antes del décimo eco y no añadió coincidencia exacta. Ambas filas R02 son autocorrelacionadas del mismo episodio, con `icmp_ratio_10s=1`. Estado global 34/145, R02 5/29, cero inválidas/advertencias y tres coincidencias DNS dentro de `train`. Claude aceptó y explicó correctamente la fase; se matizó su afirmación absoluta sobre ausencia de sesgo. Evidencia: `docs/07-dataset-campanas/42-canario-PING-10-R02.md` y `docs/04-revisiones-claude/2026-07-27-canario-PING-10-R02.md`. Siguiente: preflight individual de `PING-100/R02`.
- `F1N-PING-100-R02` fue aceptada con limitaciones: cien echo request/reply, PCAP 200/200, cero pérdida/drops, EVE 112/112 con cien alertas permitidas SID `1000001` y tres filas 48/96/56. R01 tuvo cuatro filas 6/96/96/2; la fase cambió las parciales, mientras la ventana estable de 96 coincide exactamente entre repeticiones. Estado global 35/145, R02 6/29, cero inválidas/advertencias, cuatro coincidencias dentro de `train` y cero entre particiones. Claude aceptó; se corrigieron el tipo de la cuarta coincidencia, los 23 perfiles pendientes y el posible efecto de fase sobre el peso. Evidencia: `docs/07-dataset-campanas/43-canario-PING-100-R02.md` y `docs/04-revisiones-claude/2026-07-27-canario-PING-100-R02.md`. Siguiente: preflight individual de `HTTP-10MB/R02`.
- `F1N-HTTP-10MB-R02` fue aceptada con limitaciones: HTTP 200, 10 MiB completos en 4.513970 s, PCAP 9,762/9,762, cero drops, EVE 12/12 y dos filas 9,758/4. El 74.1959 % de paquetes IPv4 cae entre 500–1500 bytes, frente a 91.6077 % en R01; ambas conservan unos 7,24 mil paquetes de 1500 bytes, pero R02 tiene más pequeños sin causa atribuida. Estado global 36/145, R02 7/29, cero inválidas/advertencias y cuatro coincidencias dentro de `train`. Claude aceptó; se descartaron causas TCP no demostradas y su conteo global erróneo. Evidencia: `docs/07-dataset-campanas/44-canario-HTTP-10MB-R02.md` y `docs/04-revisiones-claude/2026-07-27-canario-HTTP-10MB-R02.md`. Siguiente: preflight individual de `HTTP-100MB/R02`.
- `F1N-HTTP-100MB-R02` fue aceptada con limitaciones: HTTP 200, 100 MiB completos en 9.525596 s, PCAP 76,324/76,324, cero drops, EVE 14/14 y dos filas 76,319/5. Contiene 72,493 paquetes legítimos de 500–1500 bytes (94.9806 %), frente a 72,482 (91.6172 %) en R01; cambia la cantidad de paquetes pequeños y el reparto por ventanas sin causa TCP atribuida. `fileinfo` truncado a 102,400 limita Suricata, no curl/PCAP. Estado 37/145, R02 8/29, cero inválidas/advertencias y cuatro coincidencias dentro de `train`. Claude aceptó; se corrigieron su umbral inventado, lectura de filas y gates nuevos. Evidencia: `docs/07-dataset-campanas/45-canario-HTTP-100MB-R02.md` y `docs/04-revisiones-claude/2026-07-27-canario-HTTP-100MB-R02.md`. Siguiente: `HTTP-500MB/R02`.
- `F1N-HTTP-500MB-R02` fue aceptada con limitaciones: HTTP 200, 500 MiB completos en 24.506280 s, dos PCAP con 367,147/367,147 paquetes, cero drops, EVE 18/18 y cuatro filas 16,283/150,311/145,764/54,789. Contiene 362,395 paquetes legítimos de 500–1500 bytes (98.7057 %), frente a 362,387 (98.3499 %) en R01. La fase cambia el reparto sin coincidencias nuevas. `fileinfo` truncado limita Suricata, no la transferencia. Estado 38/145, R02 9/29, cero inválidas/advertencias y cuatro coincidencias dentro de `train`. Claude aceptó; se corrigieron conteos, causas y umbrales inventados. Evidencia: `docs/07-dataset-campanas/46-canario-HTTP-500MB-R02.md` y `docs/04-revisiones-claude/2026-07-27-canario-HTTP-500MB-R02.md`. Siguiente: `HTTP-1GB/R02`.
- `F1N-HTTP-1GB-R02` fue aceptada con limitaciones: HTTP 200, 1 GiB completo en 51.022895 s, tres PCAP con 749,878/749,878 paquetes, cero drops, EVE 26/26 y seis filas. Contiene 742,164 paquetes legítimos de 500–1500 bytes (98.9713 %), frente a 742,178 (98.7155 %) en R01. Volumen/duración son estables y la fase redistribuye las ventanas sin coincidencias nuevas. `fileinfo` truncado limita Suricata, no transferencia. Estado 39/145, R02 10/29, cero inválidas/advertencias y cuatro coincidencias dentro de `train`. Claude aceptó; se corrigieron causas, conteos, estado y expectativas fuera del esquema. Evidencia: `docs/07-dataset-campanas/47-canario-HTTP-1GB-R02.md` y `docs/04-revisiones-claude/2026-07-27-canario-HTTP-1GB-R02.md`. Siguiente: `HTTPS-10MB/R02`.
- `F1N-HTTPS-10MB-R02` fue aceptada con limitaciones: HTTPS 200, 10 MiB completos en 4.519138 s, PCAP 8,175/8,175, cero drops, EVE 11/11 con una sesión TLS 1.3 y una fila elegible. R01/R02 conservan 7,259/7,261 paquetes de 500–1500 bytes; R02 suma 565 pequeños y baja de 95.4127 % a 88.8196 %, sin causa TCP atribuida. R01 cruzó un borde UTC y produjo dos filas; R02 quedó en una. Certificado autofirmado y `--insecure` representan solo laboratorio; los ceros HTTP significan opacidad por cifrado. Estado 40/145, R02 11/29, cero inválidas/advertencias y cuatro coincidencias dentro de `train`. Claude aceptó; se corrigieron `fileinfo`, fase, rango inventado, techo TCP y causa del `flow`. Evidencia: `docs/07-dataset-campanas/48-canario-HTTPS-10MB-R02.md` y `docs/04-revisiones-claude/2026-07-27-canario-HTTPS-10MB-R02.md`. Siguiente: `HTTPS-100MB/R02`.
- `F1N-HTTPS-100MB-R02` fue aceptada con limitaciones: HTTPS 200, 100 MiB completos en 9.529980 s, PCAP 76,721/76,721, cero drops, EVE 12/12 con TLS 1.3 y dos filas 42,762/33,959. R01/R02 contienen 72,575/72,590 paquetes de 500–1500 bytes; R02 suma 1,848 pequeños y baja de 96.9502 % a 94.6156 %, sin causa atribuida. Ambas cruzan un borde UTC de 10 s con distinta fase y no hay coincidencias nuevas. Certificado autofirmado y `--insecure` representan laboratorio; HTTP permanece opaco. Estado 41/145, R02 12/29, cero inválidas/advertencias y cuatro coincidencias dentro de `train`. Claude aceptó; se corrigieron cálculos, ventana, fechas, causa de paquetes, perfil y gate inventado. Evidencia: `docs/07-dataset-campanas/49-canario-HTTPS-100MB-R02.md` y `docs/04-revisiones-claude/2026-07-27-canario-HTTPS-100MB-R02.md`. Siguiente: `HTTPS-500MB/R02`.
- `F1N-HTTPS-500MB-R02` fue aceptada con limitaciones: HTTPS 200, 500 MiB completos en 24.529135 s, dos PCAP con 372,404/372,404 paquetes, cero drops, EVE 17/17 con TLS 1.3 y cuatro filas 67,823/152,871/146,520/5,190. R01/R02 contienen 362,907/362,901 paquetes de 500–1500 bytes; R02 suma 972 pequeños y baja de 97.7033 % a 97.4482 %, sin causa atribuida. La fase produce tres filas R01 frente a cuatro R02, sin coincidencias nuevas. Certificado autofirmado y `--insecure` representan laboratorio; HTTP permanece opaco. Estado 42/145, R02 13/29, cero inválidas/advertencias y cuatro coincidencias dentro de `train`. Claude aceptó; se corrigieron unidad, tasa TLS, causas, transacciones y gates inventados. Evidencia: `docs/07-dataset-campanas/50-canario-HTTPS-500MB-R02.md` y `docs/04-revisiones-claude/2026-07-27-canario-HTTPS-500MB-R02.md`. Siguiente: `HTTPS-1GB/R02`.
- `F1N-HTTPS-1GB-R02` fue aceptada con limitaciones: HTTPS 200, 1 GiB completo en 51.029984 s, tres PCAP con 758,673/758,673 paquetes, cero drops, EVE 25/25 con TLS 1.3, un flow TCP y un control IPv6 fuera de alcance, y seis filas elegibles. R01/R02 contienen 743,169/743,214 paquetes de 500–1500 bytes; R02 suma 629 pequeños y baja de 98.0435 % a 97.9624 %, sin causa atribuida. La fase produce siete filas R01 frente a seis R02, sin coincidencias nuevas. Estado 43/145, R02 14/29, cero inválidas/advertencias y cuatro coincidencias dentro de `train`. Quedan 15 perfiles R02 y 102 celdas globales. Claude aceptó; se corrigieron `fileinfo`, partición, margen y control IPv6. Evidencia: `docs/07-dataset-campanas/51-canario-HTTPS-1GB-R02.md` y `docs/04-revisiones-claude/2026-07-27-canario-HTTPS-1GB-R02.md`. Siguiente: `HTTP-404-5/R02`.
- `F1N-HTTP-404-5-R02` fue aceptada con limitaciones: cinco requests produjeron cinco HTTP 404 y cinco `fileinfo` cerrados de 162 bytes; PCAP 50/50, cero drops, EVE 20/20 y una fila con `http_error_ratio_60s=1`. R01 tiene totales idénticos pero dos filas 10/40 por cruzar el borde UTC; no hay coincidencia exacta. La ruta de preflight no contaminó la evidencia. Estado 44/145, R02 15/29, cero inválidas/advertencias y cuatro coincidencias dentro de `train`. PCAP F1 actuales 10.14 GB —11.03 GB al incluir calibraciones—, proyección F1 33.67 GB y features F1 97,702 bytes. Claude aceptó; se corrigieron paquetes/eventos, flags, TLS, autocorrelación y vectores. Evidencia: `docs/07-dataset-campanas/52-canario-HTTP-404-5-R02.md` y `docs/04-revisiones-claude/2026-07-27-canario-HTTP-404-5-R02.md`. Siguiente: `TLS-SESSIONS-20/R02`.
- `F1N-TLS-SESSIONS-20-R02` fue aceptada con limitaciones: veinte sesiones secuenciales devolvieron HTTP 200 y produjeron veinte TLS 1.3 con fingerprints iguales; PCAP 430/430, cero drops, EVE 30/30 y una fila con attempts/SYN 20, tasas 2/s y `tls_session_rate_60s=20/60`. R01 produjo 431 paquetes y dos filas 324/107 al cruzar un borde; R02 quedó en una sin coincidencia exacta. Estado 45/145, R02 16/29, cero inválidas/advertencias y cuatro coincidencias dentro de `train`. Claude aceptó; se corrigieron conteo esperado, `large_ip_ratio`, timings ajenos, HTTP/2 y umbrales de recursos. Evidencia: `docs/07-dataset-campanas/53-canario-TLS-SESSIONS-20-R02.md` y `docs/04-revisiones-claude/2026-07-28-canario-TLS-SESSIONS-20-R02.md`. Siguiente: `HTTP-MULTI-1/R02`.
- `F1N-HTTP-MULTI-1-R02` fue aceptada con limitaciones: una solicitud a cada VIP `.10/.11/.12` produjo tres HTTP 200 y tres `fileinfo`; PCAP 30/30, cero drops, EVE 16/16 y una fila con `unique_dst_ip_ratio_30s=1`. El vector coincide exactamente con R01, pero los artefactos son independientes; es la quinta coincidencia dentro de `train` y no existe cruce de partición. Estado 46/145, R02 17/29, cero inválidas/advertencias. Claude aceptó; se corrigieron packet rate, duplicados, autocorrelación, porcentajes y gates inventados. Evidencia: `docs/07-dataset-campanas/54-canario-HTTP-MULTI-1-R02.md` y `docs/04-revisiones-claude/2026-07-28-canario-HTTP-MULTI-1-R02.md`. Siguiente: `HTTP-MULTI-5/R02`.
- `F1N-HTTP-MULTI-5-R02` fue aceptada con limitaciones: cinco solicitudes por cada VIP `.10/.11/.12` produjeron quince HTTP 200 y quince `fileinfo`; PCAP 150/150, cero drops, EVE 40/40 y una fila con `unique_dst_ip_ratio_30s=0.2`. R01/R02 reproducen el mismo vector exacto con artefactos independientes; es la sexta coincidencia dentro de `train` y no existe cruce de partición. Estado 47/145, R02 18/29, cero inválidas/advertencias y 98 celdas faltantes. Claude aceptó; se corrigieron IP física, partición, unidades, inode, soporte de features y validez global prematura. Evidencia: `docs/07-dataset-campanas/55-canario-HTTP-MULTI-5-R02.md` y `docs/04-revisiones-claude/2026-07-28-canario-HTTP-MULTI-5-R02.md`. Siguiente: `HTTP-C2/R02`.
- `F1N-HTTP-C2-R02` fue aceptada con limitaciones: dos descargas concurrentes de 100 MiB se solaparon durante 9.51 s; PCAP 154,784/154,784, cero drops, EVE 16/16 y 93.6718 % de paquetes entre 500–1500 bytes. Produjo dos filas: 154,778 paquetes de carga y seis de teardown tras el borde UTC. Frente a R01 hay 3,317 paquetes adicionales, principalmente pequeños, sin causa atribuida y sin vector exacto repetido. Estado 48/145, R02 19/29, cero inválidas/advertencias y seis coincidencias `train`. Claude aceptó; se corrigieron escenario C4, almacenamiento, umbrales y causalidad inventados. Evidencia: `docs/07-dataset-campanas/56-canario-HTTP-C2-R02.md` y `docs/04-revisiones-claude/2026-07-28-canario-HTTP-C2-R02.md`. Siguiente: `HTTP-C4/R02`.
- `F1N-HTTP-C4-R02` fue aceptada con limitaciones: cuatro descargas concurrentes de 100 MiB se solaparon durante 19.51 s; PCAP 310,555/310,555, cero drops, EVE 22/22 y 93.6295 % de paquetes entre 500–1500 bytes. Produjo tres filas del mismo episodio, incluida una tercera con 8,254 paquetes de carga final. Frente a R01 suma 9,038 paquetes, principalmente pequeños, y la CPU máxima pasó de 43.41 % a 72.24 %, sin causa atribuida ni pérdidas. Estado 49/145, R02 20/29, cero inválidas/advertencias y seis coincidencias `train`. Claude aceptó; se corrigieron proyecciones, umbrales, aritmética y cierre de fase inventados. Evidencia: `docs/07-dataset-campanas/57-canario-HTTP-C4-R02.md` y `docs/04-revisiones-claude/2026-07-28-canario-HTTP-C4-R02.md`. Siguiente: `HTTP-C8/R02`.
- `F1N-HTTP-C8-R02` fue aceptada con limitaciones: ocho descargas concurrentes de 100 MiB se solaparon durante 49.53 s; dos PCAP sumaron 888,265,804 bytes y 592,548/592,548 paquetes, cero drops, EVE 37/37 y 97.8996 % de paquetes entre 500–1500 bytes. Produjo seis filas del mismo episodio. Frente a R01 hay 7,580 paquetes menos, principalmente pequeños, y CPU máxima 47.29 % frente a 31.53 %, sin causa atribuida. Estado 50/145, R02 21/29, cero inválidas/advertencias, una calibración excluida y seis coincidencias `train`. Claude aceptó; se corrigieron unidades, tolerancias, duplicados, independencia y condiciones inventadas. Evidencia: `docs/07-dataset-campanas/58-canario-HTTP-C8-R02.md` y `docs/04-revisiones-claude/2026-07-28-canario-HTTP-C8-R02.md`. Siguiente: `TCP-REFUSED-5/R02`.
- `F1N-TCP-REFUSED-5-R02` fue aceptada con limitaciones: cinco intentos a `10.30.0.10:65000` produjeron exactamente cinco SYN y cinco RST/ACK en 2.427005 s; PCAP 10/10, cero drops, EVE 10/10 `stats` y dos filas elegibles. R01/R02 reproducen el resultado funcional, pero su reparto temporal 2/8 frente a 6/4 evita coincidencias exactas. Estado 51/145, R02 22/29, cero inválidas/advertencias, una calibración excluida y seis coincidencias `train`. Claude aceptó; se corrigieron generalización RST, contadores, alertas, filas, unidades, particiones y gates inventados. Evidencia: `docs/07-dataset-campanas/59-canario-TCP-REFUSED-5-R02.md` y `docs/04-revisiones-claude/2026-07-28-canario-TCP-REFUSED-5-R02.md`. Siguiente: `TCP-50M/R02`.
- `F1N-TCP-50M-R02` fue aceptada con limitaciones: iperf3 transfirió 125,042,688 bytes iguales en ambos extremos a 50.013027/50.005689 Mbit/s, sin retransmisiones; PCAP 90,134/90,134, cero drops, EVE 18/18 y 96.3166 % de paquetes entre 500–1500 bytes. Conserva la alerta/anomalía permitida de clasificación L7 y tres `flow` diferidos fuera del episodio. Produjo tres filas; frente a R01 hay 698 paquetes menos y distinta alineación, sin coincidencia exacta. Estado 52/145, R02 23/29, cero inválidas/advertencias y seis coincidencias `train`. Claude aceptó; se corrigieron capa, causalidad, timestamps, intentos, recursos y gates inventados. Evidencia: `docs/07-dataset-campanas/60-canario-TCP-50M-R02.md` y `docs/04-revisiones-claude/2026-07-28-canario-TCP-50M-R02.md`. Siguiente: `TCP-100M/R02`.
- `F1N-TCP-100M-R02` fue aceptada con limitaciones: iperf3 transfirió 250,085,376 bytes iguales en ambos extremos a 100.026788/100.024548 Mbit/s, sin retransmisiones; PCAP 181,650/181,650, cero drops, EVE 16/16 y 95.5827 % de paquetes entre 500–1500 bytes. Conserva la alerta/anomalía permitida de clasificación sobre la conexión de datos y produjo tres ventanas correlacionadas. Frente a R01 hay 34 paquetes menos y cuatro retransmisiones menos, sin causa ni tendencia atribuida y sin coincidencia exacta. Estado 53/145, R02 24/29, cero inválidas/advertencias y seis coincidencias `train`. Claude aceptó; se corrigieron tolerancias, normalidad, memoria, conteo de perfiles y condiciones inventadas. Evidencia: `docs/07-dataset-campanas/61-canario-TCP-100M-R02.md` y `docs/04-revisiones-claude/2026-07-28-canario-TCP-100M-R02.md`. Siguiente: `TCP-200M/R02`.
- `F1N-TCP-200M-R02` fue aceptada con limitaciones y cerró la progresión TCP R02: 500,039,680 bytes iguales por extremo a 199.999302/199.988834 Mbit/s, dos retransmisiones, dos PCAP con 368,896/368,896 paquetes, cero drops, EVE 17/17 y 94.1073 % entre 500–1500 bytes. Produjo tres ventanas correlacionadas. Frente a R01 suma 2,562 paquetes, principalmente pequeños, y reduce tres retransmisiones, sin causa ni tendencia atribuida. Estado 54/145, R02 25/29, cero inválidas/advertencias y seis coincidencias `train`. Claude aceptó; se corrigieron inversión PCAP/payload, RSS, causas, normalidad, tolerancias, vectores y proyecciones inventadas. Evidencia: `docs/07-dataset-campanas/62-canario-TCP-200M-R02.md` y `docs/04-revisiones-claude/2026-07-28-canario-TCP-200M-R02.md`. Siguiente: `UDP-10M/R02`.
- `F1N-UDP-10M-R02` fue aceptada con limitaciones: 25,002,616 bytes y 17,267 datagramas iguales por extremo a 10.000233/10.000011 Mbit/s, jitter 0.049850 ms, cero pérdida/reordenamiento y cero drops. PCAP 17,296/17,296, EVE 12/12 `stats` y 99.8323 % de paquetes entre 500–1500 bytes. Produjo tres ventanas correlacionadas; la interior de 8,633 paquetes coincide exactamente con R01 y añade la séptima coincidencia `train`, sin cruce de partición. Estado 55/145, R02 26/29, cero inválidas/advertencias. Claude aceptó; se corrigieron tolerancias, payload, `seed`, regresión, causas, umbrales y expectativas UDP-25M inventadas. Evidencia: `docs/07-dataset-campanas/63-canario-UDP-10M-R02.md` y `docs/04-revisiones-claude/2026-07-28-canario-UDP-10M-R02.md`. Siguiente: `UDP-25M/R02`.
- `F1N-UDP-25M-R02` fue aceptada con limitaciones: 62,504,368 bytes y 43,166 datagramas iguales por extremo a 24.999658/24.999102 Mbit/s, jitter 0.040616 ms, cero pérdida/reordenamiento y cero drops. PCAP 43,195/43,195, EVE 12/12 `stats` y 99.9329 % entre 500–1500 bytes. Los totales de payload, paquetes, PCAP y EVE son idénticos a R01, pero la fase produce filas distintas y no añade duplicado. Estado 56/145, R02 27/29, cero inválidas/advertencias y siete coincidencias `train`. Claude aceptó; se corrigieron composición UDP, capas, bitrate, normalidad, causas, ratios, umbrales y preflight futuro prematuro. Evidencia: `docs/07-dataset-campanas/64-canario-UDP-25M-R02.md` y `docs/04-revisiones-claude/2026-07-28-canario-UDP-25M-R02.md`. Siguiente: `UDP-50M/R02`.
- `F1N-UDP-50M-R02` fue aceptada con limitaciones: el emisor informó 125,007,288 bytes/86,331 datagramas y el receptor 125,005,840/86,330, mientras iperf3 3.20 declaró pérdida cero de forma contradictoria. El PCAP Sensor conserva secuencias 1–86,331 completas, 86,360/86,360 paquetes, cero drops, EVE 13 `stats` y 99.9664 % entre 500–1500 bytes. La FAQ oficial documenta datos en vuelo al cierre y 3.21 corrige un caso de pérdida UDP cero mal informada; no se atribuye causa exacta. Estado 57/145, R02 28/29, cero inválidas/advertencias y siete coincidencias `train`. Claude aceptó; se corrigieron causalidad, alcance, representatividad y relevancia de extremo. Evidencia: `docs/07-dataset-campanas/65-canario-UDP-50M-R02.md` y `docs/04-revisiones-claude/2026-07-28-canario-UDP-50M-R02.md`. Siguiente: `MIXED-LIGHT/R02`.
- `F1N-MIXED-LIGHT-R02` fue aceptada con limitaciones y completó R02 29/29: HTTP 200/104,857,600 bytes, iperf3 62,521,344 bytes iguales con cero retransmisiones y veinte pares DNS `NOERROR`. Los componentes comenzaron dentro de 35.032 ms y compartieron 0.433830 s. PCAP 123,919/123,919, cero drops, 93.5183 % entre 500–1500 bytes y EVE 57/57. Tres filas correlacionadas combinan 23 intentos, 3 SYN completos, HTTP y DNS. Estado 58/145, cero inválidas/advertencias, siete coincidencias `train` y cero entre particiones. Claude aceptó; se corrigieron solapamiento, tolerancias/causas, normalidad, independencia, filas y propuestas estadísticas improcedentes. Evidencia: `docs/07-dataset-campanas/66-canario-MIXED-LIGHT-R02.md` y `docs/04-revisiones-claude/2026-07-28-canario-MIXED-LIGHT-R02.md`. Siguiente: auditoría agregada R02; no R03.
- La auditoría agregada R02 pasó el gate reproducible en 29/29 perfiles: 75 filas, 4,389,895 observaciones de paquete, 390 de aplicación y 6,514,443,996 bytes PCAP; 14/14 features tienen soporte, distribuidas en 6 L3, 5 L4 y 3 L7. Web/TLS aporta 56 % de filas y seis PCAP concentran 73.742210 % del volumen. R02 no tiene vectores exactos internos; existen siete coincidencias R01↔R02 dentro de `train` y ninguna entre particiones, todavía inexistentes para R04/R05. Estado global 58/145, cero inválidas/advertencias. Claude declaró apto con condiciones; se corrigieron balance, detectabilidad, suficiencia, plazos, construcción prematura y métricas inventadas. iperf3 3.20 queda congelado hasta R05 con auditoría UDP reforzada; 3.21 se evaluará después en fase versionada. Evidencia: `docs/07-dataset-campanas/67-auditoria-agregada-R02-F1.md` y `docs/04-revisiones-claude/2026-07-28-auditoria-agregada-R02-F1.md`. R02 cerrada; siguiente autorizado: preparación/preflight de `DNS-VALID-10/R03`, no entrenamiento.
- `F1N-DNS-VALID-10-R03` fue aceptada con limitaciones: diez consultas produjeron diez respuestas `NOERROR` a `10.30.0.10`; PCAP 20/20/20, 2,324 bytes, cero drops y EVE 29/29. La fila elegible coincide exactamente con R01/R02, pero los artefactos, hashes y tiempos son distintos. El patrón aumenta su peso dentro de `train` sin demostrar diversidad ni reutilización. Estado 59/145, R03 1/29, cero inválidas/advertencias, ocho coincidencias `train` y cero cruces observados; validation/test aún no existen. Claude aceptó; se corrigieron tolerancias, soporte, conteos, autocorrelación, robustez, causalidad, suficiencia, particiones, umbrales y PCA/t-SNE improcedente. Evidencia: `docs/07-dataset-campanas/68-canario-DNS-VALID-10-R03.md` y `docs/04-revisiones-claude/2026-07-29-canario-DNS-VALID-10-R03.md`. Siguiente: preflight independiente de `DNS-VALID-200/R03`.
- `F1N-DNS-VALID-200-R03` fue aceptada con limitaciones: 200 consultas/respuestas `NOERROR`, PCAP 400/400/400, 46,024 bytes, cero drops y EVE 409/409. Dos filas correlacionadas 64/336 difieren de R01 228/172 y R02 24/376 por fase UTC; no añaden vector exacto. R03 usó 200 puertos distintos. Estado 60/145, R03 2/29, cero inválidas/advertencias y ocho coincidencias `train`. Claude aceptó; se corrigieron puerto reutilizado/fijo, diversidad y separabilidad inventadas. Evidencia: `docs/07-dataset-campanas/69-canario-DNS-VALID-200-R03.md` y `docs/04-revisiones-claude/2026-07-29-canario-DNS-VALID-200-R03.md`. Siguiente: preflight independiente de `DNS-MIXED-20-2/R03`.
- `F1N-DNS-MIXED-20-2-R03` fue aceptada con limitaciones: veinte consultas válidas y dos NXDOMAIN produjeron 22 pares DNS, PCAP 44/44/44, 5,092 bytes, cero drops, EVE 53/53 y una fila elegible. El vector coincide exactamente con R01/R02, aunque PCAP, EVE, manifiestos, ledgers, tiempos y hashes son independientes; aumenta el peso de esa firma en `train` sin aportar diversidad ni demostrar separabilidad. Estado 61/145, R03 3/29, cero inválidas/advertencias, nueve coincidencias `train` y cero cruces observados. Claude aceptó; se corrigieron porcentajes/totales históricos, severidad de sobreajuste no medida y diversidad futura anticipada. Evidencia: `docs/07-dataset-campanas/70-canario-DNS-MIXED-20-2-R03.md` y `docs/04-revisiones-claude/2026-07-29-canario-DNS-MIXED-20-2-R03.md`. Siguiente: preflight independiente de `DNS-MIXED-50-10/R03`.
- `F1N-DNS-MIXED-50-10-R03` fue aceptada con limitaciones: cincuenta consultas válidas y diez NXDOMAIN produjeron 60 pares DNS, PCAP 120/120/120, 13,866 bytes, cero drops, EVE 130/130 y una fila elegible. El vector coincide exactamente con R01/R02, con artefactos y hashes independientes; suma peso dentro de `train` sin demostrar diversidad o separabilidad. Estado 62/145, R03 4/29, cero inválidas/advertencias, diez coincidencias `train` y cero cruces observados. Claude aceptó; se corrigieron paquetes frente a eventos, integridad, fuente del orden, efecto ML y generalización de duplicados. Evidencia: `docs/07-dataset-campanas/71-canario-DNS-MIXED-50-10-R03.md` y `docs/04-revisiones-claude/2026-07-29-canario-DNS-MIXED-50-10-R03.md`. Siguiente: preflight independiente de `PING-10/R03`.
- `F1N-PING-10-R03` fue aceptada con limitaciones: diez requests/replies ICMP, PCAP 20/20/20, 2,304 bytes, cero drops y EVE 20/20 con diez alertas de telemetría más diez `stats`. Dos filas correlacionadas 6/14 difieren de PING-10 R01/R02 por fase UTC; la fila de seis paquetes coincide con el prefijo de PING-100/R01, como equivalencia observacional entre campañas independientes dentro de `train`. Estado 63/145, R03 5/29, cero inválidas/advertencias, once coincidencias `train` y cero cruces observados. Claude aceptó; se corrigieron paquetes/eventos, causalidad, rangos inventados, contaminación, soporte y efectos ML. Evidencia: `docs/07-dataset-campanas/72-canario-PING-10-R03.md` y `docs/04-revisiones-claude/2026-07-29-canario-PING-10-R03.md`. Siguiente: preflight independiente de `PING-100/R03`.
- `F1N-PING-100-R03` fue aceptada con limitaciones: cien requests/replies ICMP, PCAP 200/200/200, 22,824 bytes, cero drops y EVE 112/112 con cien alertas de telemetría y doce `stats`. Tres filas correlacionadas 62/98/40 no coinciden con PING-100 R01/R02 ni agregan duplicado. Estado 64/145, R03 6/29, cero inválidas/advertencias, once coincidencias `train` y cero cruces observados. Claude aceptó condicionalmente; se corrigieron paquetes/eventos, capa 3/L7, soporte, severidad/ML, causalidad y secuencia. Evidencia: `docs/07-dataset-campanas/73-canario-PING-100-R03.md` y `docs/04-revisiones-claude/2026-07-29-canario-PING-100-R03.md`. Siguiente: preflight independiente de `HTTP-10MB/R03`.
- `F1N-HTTP-10MB-R03` fue aceptada con limitaciones: HTTP 200 y 10,485,760 bytes, PCAP 7,823/7,823/7,823, cero drops y EVE 13/13. Incluye 7,248 paquetes de 500–1500 bytes (92.6499 %) y una fila multicapa no duplicada; `fileinfo` parcial no sustituye la prueba de descarga. Estado 65/145, R03 7/29, cero inválidas/advertencias, once coincidencias `train` y cero cruces observados. Claude aceptó; se corrigieron duplicado inexistente, causalidad TCP, paquetes/eventos, filas, tolerancias, contadores y bloqueos nuevos. Evidencia: `docs/07-dataset-campanas/74-canario-HTTP-10MB-R03.md` y `docs/04-revisiones-claude/2026-07-29-canario-HTTP-10MB-R03.md`. Siguiente: preflight independiente de `HTTP-100MB/R03`.
- `F1N-HTTP-100MB-R03` fue aceptada con limitaciones: HTTP 200 y 104,857,600 bytes, PCAP 77,196/77,196/77,196, cero drops y EVE 13/13. Incluye 72,464 paquetes de 500–1500 bytes (93.8701 %) y dos filas correlacionadas 77,189/7 sin nuevo duplicado. Estado 66/145, R03 8/29, cero inválidas/advertencias, once coincidencias `train` y cero cruces observados. Claude aceptó tras un reintento; se corrigieron paquetes/eventos, tolerancia, cola TCP, conteo pequeño, duplicados y ML. Evidencia: `docs/07-dataset-campanas/75-canario-HTTP-100MB-R03.md` y `docs/04-revisiones-claude/2026-07-30-canario-HTTP-100MB-R03.md`. Siguiente: preflight independiente de `HTTP-500MB/R03`.
- `F1N-HTTP-500MB-R03` fue aceptada con limitaciones: HTTP 200 y 524,288,000 bytes, dos PCAP con 371,273 paquetes íntegros, cero drops y EVE 17/17. Incluye 362,216 paquetes de 500–1500 bytes (97.5606 %) y tres filas correlacionadas no duplicadas. Estado 67/145, R03 9/29, cero inválidas/advertencias, once coincidencias `train` y cero cruces. Claude aceptó; se corrigieron EVE, paquetes/eventos, tolerancias, rangos, referencias, fase, recursos y ML. Evidencia: `docs/07-dataset-campanas/76-canario-HTTP-500MB-R03.md` y `docs/04-revisiones-claude/2026-07-30-canario-HTTP-500MB-R03.md`. Siguiente: preflight independiente de `HTTP-1GB/R03`.
- `F1N-HTTP-1GB-R03` fue aceptada con limitaciones: HTTP 200 y 1,073,741,824 bytes, tres PCAP con 750,723 paquetes íntegros, cero drops y EVE 26/26. Incluye 742,152 paquetes de 500–1500 bytes (98.8583 %) y seis filas correlacionadas no duplicadas. Estado 68/145, R03 10/29, cero inválidas/advertencias, once coincidencias `train` y cero cruces. Claude aceptó; se corrigieron referencias, paquetes/eventos, pequeños, HTTP/cifrado, unidades, recursos, causas, tolerancias y ML. Evidencia: `docs/07-dataset-campanas/77-canario-HTTP-1GB-R03.md` y `docs/04-revisiones-claude/2026-07-30-canario-HTTP-1GB-R03.md`. Siguiente: preflight independiente de `HTTPS-10MB/R03`.
- `F1N-HTTPS-10MB-R03` fue aceptada con limitaciones: HTTPS 200 y 10,485,760 bytes, PCAP 8,200/8,200/8,200, cero drops y EVE 12/12 con una sesión TLS 1.3 y una fila elegible. Incluye 7,256 paquetes de 500–1500 bytes (88.4878 %) y no agrega un duplicado. Estado 69/145, R03 11/29, cero inválidas/advertencias, once coincidencias `train` y cero cruces observados. Tras renovar la sesión, Claude/Sonnet aceptó con limitaciones y autorizó solo el siguiente preflight; se corrigió su atribución no demostrada del delta Suricata +3. Evidencia: `docs/07-dataset-campanas/78-canario-HTTPS-10MB-R03.md` y `docs/04-revisiones-claude/2026-07-30-canario-HTTPS-10MB-R03.md`. Siguiente: preflight independiente de `HTTPS-100MB/R03`.
- `F1N-HTTPS-100MB-R03` fue aceptada con limitaciones: HTTPS 200 y 104,857,600 bytes, PCAP 75,114/75,114/75,114, cero drops y EVE 12/12 con una sesión TLS 1.3. Incluye 72,576 paquetes de 500–1500 bytes (96.6211 %) y dos filas correlacionadas sin duplicado nuevo. Estado 70/145, R03 12/29, cero inválidas/advertencias, once coincidencias `train` y cero cruces observados. Claude/Sonnet aceptó y autorizó solo el siguiente preflight; se corrigieron paquetes/eventos, solapamiento de historias y causalidad de fase. Evidencia: `docs/07-dataset-campanas/79-canario-HTTPS-100MB-R03.md` y `docs/04-revisiones-claude/2026-07-31-canario-HTTPS-100MB-R03.md`. Siguiente: preflight independiente de `HTTPS-500MB/R03`.
- `F1N-HTTPS-500MB-R03` fue aceptada con limitaciones: HTTPS 200 y 524,288,000 bytes, dos PCAP con 371,204 paquetes íntegros, cero drops y EVE 17/17 con una sesión TLS 1.3. Incluye 362,957 paquetes de 500–1500 bytes (97.7783 %) y tres filas correlacionadas sin duplicado nuevo. Estado 71/145, R03 13/29, cero inválidas/advertencias, once coincidencias `train` y cero cruces observados. Claude/Sonnet autorizó primero una ejecución y luego aceptó; se corrigieron NTP, límite iperf3, integridad universal, recursos y particiones futuras. Evidencia: `docs/07-dataset-campanas/80-canario-HTTPS-500MB-R03.md` y `docs/04-revisiones-claude/2026-07-31-canario-HTTPS-500MB-R03.md`. Siguiente: preflight independiente de `HTTPS-1GB/R03`.
- `F1N-HTTPS-1GB-R03` fue aceptada con limitaciones y cerró los tamaños HTTPS individuales: HTTPS 200 y 1,073,741,824 bytes, tres PCAP con 758,423 paquetes íntegros, cero drops y EVE 25/25 con TLS 1.3 y flow TCP. Incluye 743,379 paquetes de 500–1500 bytes (98.0164 %) y seis filas correlacionadas sin duplicado nuevo. Estado 72/145, R03 14/29, cero inválidas/advertencias, once coincidencias `train` y cero cruces observados. Claude/Sonnet autorizó primero una ejecución y luego aceptó; se corrigieron NTP, SHA, gate de filas, historia attempts/SYN y particiones futuras. Evidencia: `docs/07-dataset-campanas/81-canario-HTTPS-1GB-R03.md` y `docs/04-revisiones-claude/2026-07-31-canario-HTTPS-1GB-R03.md`. Siguiente: preflight independiente de `HTTP-404-5/R03`.
- `F1N-HTTP-404-5-R03` fue aceptada con limitaciones: cinco GET produjeron cinco HTTP 404 y cinco `fileinfo CLOSED`; PCAP 50/50/50, cero drops, EVE 20/20 y una fila con `http_error_ratio_60s=1`. Las rutas exclusivas de preflight no aparecen. La fila coincide exactamente con R02, aunque los artefactos/hashes son independientes; las coincidencias `train` suben a doce. Estado 73/145, R03 15/29, cero inválidas/advertencias y cero cruces observados. Claude/Sonnet autorizó primero una ejecución y luego aceptó; se corrigieron NTP, secuencia temporal, fase, alcance negativo y custodia. Evidencia: `docs/07-dataset-campanas/82-canario-HTTP-404-5-R03.md` y `docs/04-revisiones-claude/2026-07-31-canario-HTTP-404-5-R03.md`. Siguiente: preflight independiente de `TLS-SESSIONS-20/R03`.

## Observaciones obligatorias del jurado

### Tráfico legítimo pesado

El dataset debe incluir tráfico legítimo con paquetes grandes, aproximadamente de 500 a 1500 bytes. Un paquete grande no puede convertirse por sí solo en señal de ataque. Se deben generar cargas legítimas reproducibles, concurrentes y variadas, separando correctamente entrenamiento, validación y prueba.

### Features multicapa

Las 14 variables de entrada deben incluir comportamiento útil de las capas 3, 4 y 7. Como mínimo se deben evaluar:

- Capa 3: ratio o cantidad de IP únicas, diversidad de origen/destino u otra medida por ventana.
- Capa 4: frecuencia o ratio de flags SYN, estados TCP y tasas por flujo/ventana.
- Capa 7: intentos fallidos de autenticación u otra señal semántica del servicio.

No aceptar una feature solo porque su nombre menciona una capa. Exigir definición matemática, ventana temporal, fuente de datos, tratamiento de valores faltantes, costo de cálculo en línea y justificación de por qué representa comportamiento.

## Reparto de responsabilidades

### Codex: implementación y operación

Codex se encarga principalmente de:

- configurar las VMs y servicios;
- crear y ejecutar playbooks de Ansible;
- implementar Suricata, captura, extracción de features y modelo;
- ejecutar pruebas positivas, negativas y de regresión;
- recolectar evidencia no sensible;
- mantener documentación y commits pequeños y trazables.

### Claude: revisión adversarial

Claude se encarga principalmente de:

- revisar arquitectura, configuración, código, dataset y metodología;
- buscar rutas que eviten el Sensor y puntos ciegos de captura;
- detectar fuga de datos, sesgo, etiquetas débiles y métricas engañosas;
- cuestionar si las 14 features son calculables en tiempo real;
- evaluar falsos positivos con tráfico legítimo pesado;
- diseñar pruebas que intenten refutar las conclusiones;
- simular observaciones y preguntas difíciles del jurado.

Claude puede implementar cambios cuando el usuario lo solicite expresamente. Antes debe explicar el alcance, evitar competir con cambios activos de Codex y trabajar en archivos o ramas claramente delimitados.

Claude también está autorizado para validar trabajo realizado por Codex y, cuando el usuario se lo pida, crear o actualizar documentación y publicarla en el repositorio oficial siguiendo el protocolo Git de este archivo.

## Formato obligatorio de las revisiones

Cada observación debe contener:

1. Identificador y título.
2. Severidad: crítica, alta, media o baja.
3. Hecho observado y evidencia concreta.
4. Inferencias separadas de los hechos.
5. Riesgo para seguridad, funcionamiento o validez científica.
6. Prueba reproducible para confirmar o refutar el problema.
7. Corrección propuesta y posibles efectos secundarios.
8. Estado: pendiente, confirmada, rechazada o corregida.

No presentar preferencias de estilo como fallos técnicos. No aceptar afirmaciones de otro agente sin comprobarlas.

## Flujo de revisión cruzada

```text
Codex implementa y entrega evidencia
                │
                ▼
Claude revisa de forma adversarial
                │
                ▼
Claude propone pruebas reproducibles
                │
                ▼
Codex reproduce y corrige hallazgos confirmados
                │
                ▼
Claude realiza la segunda revisión
```

Cada hallazgo debe terminar como confirmado mediante una prueba, rechazado mediante evidencia o pendiente con una razón concreta.

## Validación del trabajo realizado por Codex

Cuando el usuario pida revisar algo implementado por Codex, Claude debe:

1. Leer el commit, documentación y archivos relacionados.
2. Revisar `git log --oneline -10` y `git show --stat <commit>`.
3. Comparar lo documentado con la configuración o evidencia disponible.
4. Ejecutar primero verificaciones no destructivas.
5. No repetir una operación destructiva solo para comprobar que ocurrió.
6. Diseñar al menos una prueba positiva y una negativa.
7. Registrar discrepancias con el formato obligatorio de hallazgos.
8. No declarar un hallazgo corregido hasta verificarlo nuevamente.

Claude no debe aprobar automáticamente un cambio porque tenga un commit exitoso. Un commit demuestra trazabilidad, no funcionamiento.

## Publicación de documentación en GitHub

Claude puede crear y publicar documentación cuando el usuario lo autorice. El destino oficial es:

```text
origin  git@github.com:marksato13/VF-Sistema-Open-Source-para-la-Deteccion-Temprana-de-Comportamientos-Anomalos-en-Redes-de-Datos.git
```

Antes de editar o publicar:

```bash
git status --short --branch
git remote -v
git log -5 --oneline
```

Reglas de publicación:

- No usar tokens escritos en comandos, archivos o mensajes de commit.
- Utilizar la autenticación SSH ya configurada en la VM administrativa.
- No hacer `push --force`.
- No reescribir commits publicados.
- No usar `git reset --hard` ni descartar cambios ajenos.
- No publicar datasets, PCAP, logs o secretos sin revisar tamaño y contenido.
- Ejecutar `git diff --check` antes del commit.
- Crear commits pequeños con mensajes descriptivos.
- Hacer `push` solamente después de revisar el diff y cuando el usuario haya autorizado publicar.

Si el árbol está limpio y no existe trabajo concurrente, Claude puede usar una rama propia:

```bash
git switch -c claude/revision-<tema>
```

Las revisiones de Claude deben guardarse preferentemente en:

```text
docs/04-revisiones-claude/
```

Si el usuario solicita una corrección documental directa y no hay cambios concurrentes, puede actualizar el documento correspondiente. Para cambios de código, infraestructura, firewall, red, Suricata o ML, se recomienda una rama separada y revisión de Codex antes de integrar a `main`.

Después de publicar, Claude debe informar:

- rama utilizada;
- hash y mensaje del commit;
- archivos modificados;
- verificaciones ejecutadas;
- limitaciones o tareas pendientes.

## Reglas de seguridad y repositorio

- No guardar contraseñas, tokens, cookies, claves privadas ni secretos en Git.
- No repetir en archivos credenciales expuestas en conversaciones.
- Solicitar rotación de cualquier secreto expuesto.
- No cambiar simultáneamente `main` desde dos agentes.
- Antes de editar, revisar `git status` y los diffs existentes.
- No eliminar ni revertir cambios ajenos sin autorización.
- No desconectar interfaces administrativas sin acceso de recuperación por consola ESXi.
- No ejecutar tráfico ofensivo fuera de las redes y máquinas explícitamente autorizadas del laboratorio.
- Sanitizar PCAP, `eve.json`, logs y capturas antes de publicarlos.
- Separar artefactos runtime y datasets grandes del código fuente.

## Criterio de finalización

Una fase solo se considera terminada cuando existen:

1. configuración persistente;
2. prueba funcional positiva;
3. prueba negativa o de fallo;
4. evidencia con fecha y zona horaria;
5. evaluación de riesgos y limitaciones;
6. documentación reproducible;
7. commit identificable sin secretos.

El objetivo final no es demostrar que el sistema siempre tiene razón, sino delimitar con evidencia qué detecta, bajo qué condiciones funciona, cuáles son sus falsos positivos y falsos negativos, y qué limitaciones conserva.

## Rediseño experimental vigente

Antes de ejecutar escenarios, Claude debe leer `docs/05-plan-pruebas/01-diseno-defendible.md` y `docs/05-plan-pruebas/README.md`. La campaña vigente se organiza como F0 (calibración), F1 (normalidad para entrenamiento), F2 (estrés legítimo), F3 (anomalías L3/L4/L7) y F4 (mixto). No se deben ejecutar ataques ni capturar el dataset final mientras Kali no tenga NTP sincronizado y las NIC externas estén aisladas.

El propósito es producir ventanas etiquetadas y reproducibles para Isolation Forest y modelos futuros. Las features nuevas deben demostrar código, diccionario, prueba y ablación; nunca deben marcarse como implementadas solo por estar planificadas. Claude debe revisar cada campaña contra MITRE ATT&CK, NIST y la documentación oficial de Suricata, y cuestionar tamaño de muestra, contaminación entre particiones, falsos positivos y pérdida de paquetes.

VM03 dispone ahora de NGINX HTTP/HTTPS, DNS local, iperf3 y SSH/SFTP; VM05 tiene las herramientas de cliente. La prueba iperf3 sin pacing de 2.58 Gbit/s produjo 389,932 descartes y está excluida del dataset. AF_PACKET fue ajustado y 100 Mbit/s durante 10 s obtuvo cero drops. Claude debe rechazar cualquier escenario iperf3 sin bitrate explícito y recordar que fallos de login SSH no son visibles en EVE sin integrar logs del host.

La calibración posterior fijó máximos F1 de 200 Mbit/s TCP y 50 Mbit/s UDP. TCP 250 tuvo retransmisiones y solo una ronda, TCP 300 fue abortado por la guardia, y UDP 75/100 presentó pérdida. `scripts/f1/run-benign.sh` rechaza esos valores superiores. La calibración no pertenece al dataset final.

HTTP/HTTPS quedó calibrado con 10 MB, 100 MB, 500 MB y 1 GB, además de 2/4/8 flujos concurrentes. Suricata terminó con cero drops y una muestra tcpdump registró 90.84 % de payloads TCP entre 500 y 1500 bytes. Los detalles están en `08-resultados-http-https-G2.md`; estos datos también son calibración y no entrenamiento.

El cierre de `TLS-SESSIONS-20/R03` está en `docs/07-dataset-campanas/83-canario-TLS-SESSIONS-20-R03.md`: veinte sesiones TLS homogéneas, dos filas correlacionadas por borde UTC y ningún duplicado exacto nuevo. Siguiente autorizado: solo preflight independiente de `F1N-HTTP-MULTI-1-R03`.

El cierre de `HTTP-MULTI-1/R03` está en `docs/07-dataset-campanas/84-canario-HTTP-MULTI-1-R03.md`: tres VIP lógicas de una VM, una fila exacta a R01/R02 y contador global de duplicados dentro de `train` elevado a trece. Siguiente autorizado: solo preflight independiente de `F1N-HTTP-MULTI-5-R03`.

El cierre de `HTTP-MULTI-5/R03` está en `docs/07-dataset-campanas/85-canario-HTTP-MULTI-5-R03.md`: quince GET a tres VIP lógicas, vector exacto R01/R02 y contador global de duplicados dentro de `train` elevado a catorce. Siguiente autorizado: solo preflight independiente de `F1N-HTTP-C2-R03`.

El cierre de `HTTP-C2/R03` está en `docs/07-dataset-campanas/86-canario-HTTP-C2-R03.md`: dos descargas concurrentes, 144,929 paquetes legítimos de 500–1500 bytes y una fila de cierre exacta a R02. Siguiente autorizado: solo preflight independiente de `F1N-HTTP-C4-R03`.

El cierre de `HTTP-C4/R03` está en `docs/07-dataset-campanas/87-canario-HTTP-C4-R03.md`: cuatro descargas concurrentes, 290,000 paquetes legítimos de 500–1500 bytes y ningún duplicado exacto nuevo. Siguiente autorizado: solo preflight independiente de `F1N-HTTP-C8-R03`.

El cierre de `HTTP-C8/R03` está en `docs/07-dataset-campanas/88-canario-HTTP-C8-R03.md`: ocho descargas concurrentes, dos PCAP, 580,019 paquetes legítimos de 500–1500 bytes y cero drops. Siguiente autorizado: solo preflight independiente de `F1N-TCP-REFUSED-5-R03`.

El cierre de `TCP-REFUSED-5/R03` está en `docs/07-dataset-campanas/89-canario-TCP-REFUSED-5-R03.md`: cinco pares SYN–RST/ACK, una fila L4 y ningún duplicado exacto nuevo. Siguiente autorizado: solo preflight independiente de `F1N-TCP-50M-R03`.

El cierre de `TCP-50M/R03` está en `docs/07-dataset-campanas/90-canario-TCP-50M-R03.md`: transferencia íntegra a 50 Mbit/s, 86,818 paquetes legítimos de 500–1500 bytes, cuatro retransmisiones sin causa atribuida y cero drops. Siguiente autorizado: solo preflight independiente de `F1N-TCP-100M-R03`.

El cierre de `TCP-100M/R03` está en `docs/07-dataset-campanas/91-canario-TCP-100M-R03.md`: transferencia íntegra a 100 Mbit/s, 173,634 paquetes legítimos de 500–1500 bytes, siete retransmisiones sin causa atribuida y cero drops. Siguiente autorizado: solo preflight independiente de `F1N-TCP-200M-R03`.

El cierre de `TCP-200M/R03` está en `docs/07-dataset-campanas/92-canario-TCP-200M-R03.md`: techo TCP íntegro a 200 Mbit/s, dos PCAP, 347,157 paquetes legítimos de 500–1500 bytes y cero drops. Siguiente autorizado: solo preflight independiente de `F1N-UDP-10M-R03`.

El cierre de `UDP-10M/R03` está en `docs/07-dataset-campanas/93-canario-UDP-10M-R03.md`: 17,267 datagramas íntegros, cero pérdida/reordenamiento, cero drops y una fila exacta R01/R02. Siguiente autorizado: solo preflight independiente de `F1N-UDP-25M-R03`.

El cierre de `UDP-25M/R03` está en `docs/07-dataset-campanas/94-canario-UDP-25M-R03.md`: 43,166 datagramas íntegros, cero pérdida/reordenamiento, cero drops y una fila exacta R01. Siguiente autorizado: solo preflight independiente de `F1N-UDP-50M-R03`.

El cierre de `UDP-50M/R03` está en `docs/07-dataset-campanas/95-canario-UDP-50M-R03.md`: 86,329 datagramas coincidentes por extremo, secuencia binaria completa en Sensor, cero drops y ningún duplicado exacto nuevo. R03 no reproduce la discrepancia de R02, pero no prueba su causa ni la fiabilidad general de iperf3 3.20. Estado: 86/145, R03 28/29. Siguiente autorizado: solo preflight independiente de `F1N-MIXED-LIGHT-R03`.

El cierre de `MIXED-LIGHT/R03` está en `docs/07-dataset-campanas/96-canario-MIXED-LIGHT-R03.md`: HTTP, TCP y veinte DNS se solaparon, el PCAP quedó íntegro y un `flow` DNS de preflight emitido por timeout se conservó en EVE pero quedó fuera del PCAP y features. Estado: 87/145, R03 29/29, diecisiete duplicados internos sin incremento y cero cruzados. Siguiente autorizado: únicamente auditoría agregada de cierre R03; no iniciar R04.

La auditoría agregada `docs/07-dataset-campanas/97-auditoria-agregada-R03-F1.md` cierra R03 con 87 episodios, 224 ventanas train y 19.54 GB PCAP. Ninguna de las catorce features queda sin soporte; 172/224 ventanas contienen tráfico pesado legítimo. El congelamiento y la política sin deduplicación post hoc están en `docs/05-plan-pruebas/18-congelamiento-protocolo-R04-R05.md`.

El protocolo `PM-F1-v1` está publicado desde `b7769a5` y documentado en `docs/06-features-modelado/03-protocolo-modelado-F1-v2.md`: Isolation Forest queda como principal predefinido; LOF y OCSVM son comparadores; R01–R03 ajustan el pipeline, R04 completa calibra una vez un cuantil inferior con `alpha=0.05` y R05 se evalúa una vez. El verificador versionado demostró que `sample_weight=1/n` no cambió Isolation Forest 1.9.0 en tres semillas, por lo que la sensibilidad por campaña usa expansión determinista a 84 registros por episodio. Claude cerró sus dos bloqueos y declaró el protocolo apto metodológicamente en `docs/04-revisiones-claude/2026-08-04-protocolo-modelado-F1-v2.md`. No puntuar modelos durante R04.

`F1N-DNS-VALID-10-R04` fue aceptada con limitaciones: diez requests y diez responses `NOERROR` a `10.30.0.10`, PCAP 20/20/20 de 2,324 bytes, cero drops, EVE 29/29 y una fila elegible. El vector coincide exactamente con R01–R03 y se conserva como primer cruce `seen` train↔validation; no se borra ni se trata como fuga operacional. Auditor: 88/145, R04 1/29, 18 coincidencias, un cruce, cero inválidas/advertencias. Claude confirmó cifras y ausencia de scoring. Evidencia: `docs/07-dataset-campanas/98-canario-DNS-VALID-10-R04.md` y `docs/04-revisiones-claude/2026-08-04-canario-DNS-VALID-10-R04.md`. Siguiente autorizado: sólo preflight independiente de `F1N-DNS-VALID-200-R04`.

`F1N-DNS-VALID-200-R04` fue aceptada con limitaciones: 200 requests/responses `NOERROR`, PCAP 400/400/400 de 46,024 bytes, cero drops, EVE 410 y dos filas 274/126 paquetes. La segunda conserva 200 intentos/queries en historia 30/60 s pero tasa 10 s de 63 intentos; no hay contradicción de ventana. Ninguna fila coincide exactamente con train. Auditor: 89/145, R04 2/29, 18 coincidencias, un cruce, cero inválidas/advertencias. Claude aceptó; su cobertura parcial de puertos y hashes fue completada por Codex sobre PCAP/bundles íntegros. Evidencia: `docs/07-dataset-campanas/99-canario-DNS-VALID-200-R04.md` y `docs/04-revisiones-claude/2026-08-04-canario-DNS-VALID-200-R04.md`. Siguiente autorizado: sólo preflight independiente de `F1N-DNS-MIXED-20-2-R04`.

`F1N-DNS-MIXED-20-2-R04` fue aceptada con limitaciones: 22 requests/responses en orden, veinte `NOERROR` + dos NXDOMAIN, PCAP 44/44, cero drops, EVE 54 y una fila con ratio `2/22`. Sus 24 observaciones internas son 22 queries + dos marcadores NXDOMAIN. El vector coincide exactamente con R01–R03 y añade el segundo cruce `seen`. EVE conserva un flow de probe iniciado cinco minutos antes y emitido por timeout durante la captura; está fuera del PCAP y no alteró los 22 intentos/features. Auditor limpio: 90/145, R04 3/29, 19 coincidencias, dos cruces, cero inválidas/advertencias. Claude aceptó; se corrigió su generalización de precedentes R01–R03 a sólo R03 documentado. Evidencia: `docs/07-dataset-campanas/100-canario-DNS-MIXED-20-2-R04.md` y `docs/04-revisiones-claude/2026-08-04-canario-DNS-MIXED-20-2-R04.md`. Siguiente: sólo preflight de `F1N-DNS-MIXED-50-10-R04`.

`F1N-DNS-MIXED-50-10-R04` fue aceptada con limitaciones: 60 requests/responses en orden, cincuenta `NOERROR` + diez NXDOMAIN, PCAP 120/120/120 de 13,866 bytes, cero drops, EVE 130 sin flow y una fila con ratio `10/60`. Sus 70 observaciones internas son 60 queries + diez marcadores NXDOMAIN. El vector coincide exactamente con R01–R03 y añade el tercer cruce `seen`. Suricata incrementó 124 frente a 120 paquetes PCAP; los cuatro adicionales quedan sin causa atribuida. Auditor limpio: 91/145, R04 4/29, veinte coincidencias, tres cruces, cero inválidas/advertencias. Claude verificó los artefactos crudos y no encontró discrepancias; su sesión sólo lectura no pudo reejecutar el auditor, que Codex sí confirmó aparte. Evidencia: `docs/07-dataset-campanas/101-canario-DNS-MIXED-50-10-R04.md` y `docs/04-revisiones-claude/2026-08-04-canario-DNS-MIXED-50-10-R04.md`. Siguiente: sólo preflight de `F1N-PING-10-R04`; no hay scoring.

`F1N-PING-10-R04` fue aceptada con limitaciones: diez pares ICMP con 0 % de pérdida, PCAP 20/20/20 de 2,304 bytes, diez alertas SID 1000001 permitidas y dos filas 16/4 sin coincidencia exacta previa. EVE 23 conserva dos flows emitidos por timeout durante warm-up cuyos paquetes ICMP/DNS ocurrieron unos cinco minutos antes y fuera del PCAP; no entraron a features. Suricata incrementó 24 frente a PCAP 20, sin causa atribuida ni drops. Auditor limpio: 92/145, R04 5/29, veinte coincidencias y tres cruces sin incremento, cero inválidas/advertencias. Claude verificó EVE completo, contadores, features y recursos sin hallar contradicciones; Codex completó hashes y auditor no ejecutables en su sesión sólo lectura. Evidencia: `docs/07-dataset-campanas/102-canario-PING-10-R04.md` y `docs/04-revisiones-claude/2026-08-04-canario-PING-10-R04.md`. Siguiente: sólo preflight de `F1N-PING-100-R04`; no hay scoring.

`F1N-PING-100-R04` fue aceptada con limitaciones: cien pares ICMP sin pérdida, PCAP 200/200/200 de 22,824 bytes, cien alertas SID 1000001 permitidas y tres filas 76/98/26. EVE 111 no contiene evento flow ni anomalía. La ventana central 98 coincide exactamente con PING-100/R03 y añade el cuarto cruce `seen`; las otras dos son nuevas. Suricata incrementó 204 frente a PCAP 200, sin causa atribuida ni drops. Auditor limpio: 93/145, R04 6/29, 21 coincidencias, cuatro cruces y cero inválidas/advertencias. Claude verificó artefactos y aceptó; se corrigió su lapsus sobre el estado previo, que era 20 coincidencias/tres cruces y no 20/cuatro. Evidencia: `docs/07-dataset-campanas/103-canario-PING-100-R04.md` y `docs/04-revisiones-claude/2026-08-04-canario-PING-100-R04.md`. Siguiente: sólo preflight de `F1N-HTTP-10MB-R04`; no hay scoring.

`F1N-HTTP-10MB-R04` fue aceptada: descargó 10,485,760 bytes con HTTP 200, PCAP 7,829/7,829/7,829 y 7,245 paquetes legítimos de 500–1500 bytes. Produjo una fila nueva; EVE contiene HTTP+fileinfo+stats y el fileinfo truncado corresponde al límite de inspección, no a pérdida de descarga. Auditor limpio: 94/145, R04 7/29, 21 coincidencias, cuatro cruces y cero inválidas/advertencias. Claude verificó artefactos sin discrepancias. Evidencia: `docs/07-dataset-campanas/104-canario-HTTP-10MB-R04.md` y `docs/04-revisiones-claude/2026-08-04-canario-HTTP-10MB-R04.md`. Siguiente: sólo preflight HTTP-100MB/R04; no hay scoring.

`F1N-HTTP-100MB-R04` fue aceptada con limitaciones: descargó 104,857,600 bytes con HTTP 200, PCAP 78,509/78,509/78,509 y 72,459 paquetes legítimos de 500–1500 bytes. Produjo dos filas nuevas 77,544/965; EVE contiene HTTP+fileinfo+stats y no flow. Suricata incrementó 78,513 frente a PCAP 78,509 sin drops. Auditor limpio: 95/145, R04 8/29, 21 coincidencias, cuatro cruces y cero inválidas/advertencias. Claude contrastó R01–R04 sin pedir correcciones. Evidencia: `docs/07-dataset-campanas/105-canario-HTTP-100MB-R04.md` y `docs/04-revisiones-claude/2026-08-04-canario-HTTP-100MB-R04.md`. Siguiente: sólo preflight HTTP-500MB/R04; no hay scoring.

`F1N-HTTP-500MB-R04` fue aceptada con limitaciones: completó una transferencia HTTP 200 de 524,288,000 bytes, dos PCAP íntegros con 371,072 paquetes y cero drops, 362,240 paquetes de 500–1500 bytes y tres filas nuevas. El primer bloque de bypass fue invalidado por una redirección `22>` defectuosa y se repitió correctamente antes de abrir la campaña; no creó artefactos. Auditor limpio: 96/145, R04 9/29, 21 coincidencias, cuatro cruces y cero inválidas/advertencias. Claude verificó manifest, ledger, CSV y comparación R01–R03 sin hallar discrepancias; Codex completó hashes, recursos y auditor fuera de su sesión Read. Evidencia: `docs/07-dataset-campanas/106-canario-HTTP-500MB-R04.md` y `docs/04-revisiones-claude/2026-08-04-canario-HTTP-500MB-R04.md`. Siguiente: sólo preflight HTTP-1GB/R04; no hay scoring.

`F1N-HTTP-1GB-R04` fue aceptada con limitaciones: completó una transferencia HTTP 200 de 1,073,741,824 bytes, tres PCAP íntegros con 751,698 paquetes y cero drops, 742,012 paquetes de 500–1500 bytes y seis filas nuevas. Claude rechazó un preflight fragmentado; Codex repitió todos los gates en un proceso continuo y Claude autorizó una única captura. EVE conserva dos flows de sondas previas emitidos por timeout, fuera del PCAP/features, además del flow real. Auditor limpio: 97/145, R04 10/29, 21 coincidencias, cuatro cruces y cero inválidas/advertencias. Claude contrastó los artefactos primarios sin encontrar discrepancias; Codex completó EVE y auditor fuera de su sesión Read. Evidencia: `docs/07-dataset-campanas/107-canario-HTTP-1GB-R04.md` y `docs/04-revisiones-claude/2026-08-04-canario-HTTP-1GB-R04.md`. Siguiente: sólo preflight HTTPS-10MB/R04; no hay scoring.

`F1N-HTTPS-10MB-R04` fue aceptada con limitaciones: completó una transferencia HTTPS 200 de 10,485,760 bytes, PCAP íntegro con 7,942 paquetes y cero drops, 7,258 paquetes de 500–1500 bytes y una fila TLS nueva. Un preflight con quoting remoto defectuoso fue invalidado antes de captura; la repetición completa pasó y Claude autorizó una única ejecución. EVE contiene TLS 1.3 + stats, sin HTTP/fileinfo por cifrado. Auditor limpio: 98/145, R04 11/29, 21 coincidencias, cuatro cruces y cero inválidas/advertencias. Claude aceptó por consistencia documental, pero su sandbox no pudo abrir los artefactos; Codex verificó evidencia primaria, hashes y auditor. Evidencia: `docs/07-dataset-campanas/108-canario-HTTPS-10MB-R04.md` y `docs/04-revisiones-claude/2026-08-04-canario-HTTPS-10MB-R04.md`. Siguiente: sólo preflight HTTPS-100MB/R04; no hay scoring.

`F1N-HTTPS-100MB-R04` fue aceptada con limitaciones: completó HTTPS 200/104,857,600 bytes, PCAP 76,726 íntegro, cero drops, 72,561 paquetes pesados y dos filas nuevas. EVE contiene TLS 1.3 + stats y conserva opacidad HTTP. Auditor limpio: 99/145, R04 12/29, 21 coincidencias, cuatro cruces y cero inválidas/advertencias. Claude confirmó consistencia documental; Codex verificó artefactos y auditor. Evidencia: `docs/07-dataset-campanas/109-canario-HTTPS-100MB-R04.md` y `docs/04-revisiones-claude/2026-08-05-canario-HTTPS-100MB-R04.md`. Siguiente: sólo preflight HTTPS-500MB/R04; no hay scoring.

`F1N-HTTPS-500MB-R04` fue aceptada con limitaciones: completó HTTPS 200/524,288,000 bytes, dos PCAP íntegros con 375,137 paquetes, cero drops, 362,741 paquetes pesados y cuatro filas nuevas. EVE conserva dos flows de preflight fuera del PCAP/features y una sesión TLS real. Auditor limpio: 100/145, R04 13/29, 21 coincidencias, cuatro cruces y cero inválidas/advertencias. Claude confirmó consistencia documental; Codex verificó artefactos y auditor. Evidencia: `docs/07-dataset-campanas/110-canario-HTTPS-500MB-R04.md` y `docs/04-revisiones-claude/2026-08-05-canario-HTTPS-500MB-R04.md`. Siguiente: sólo preflight HTTPS-1GB/R04; no hay scoring.

`F1N-HTTPS-1GB-R04` fue aceptada con limitaciones: completó HTTPS 200/1,073,741,824 bytes, tres PCAP íntegros con 759,557 paquetes, cero drops, 743,106 paquetes pesados y seis filas nuevas. Un preflight TCP/22 defectuoso fue invalidado y repetido completo antes de captura. EVE conserva tres flows previos fuera del PCAP/features, TLS y el flow real exacto. Auditor limpio: 101/145, R04 14/29, 21 coincidencias, cuatro cruces y cero inválidas/advertencias. Claude confirmó consistencia documental; Codex verificó artefactos y auditor. Evidencia: `docs/07-dataset-campanas/111-canario-HTTPS-1GB-R04.md` y `docs/04-revisiones-claude/2026-08-05-canario-HTTPS-1GB-R04.md`. Siguiente: sólo preflight HTTP-404-5/R04; no hay scoring.

`F1N-HTTP-404-5-R04` fue aceptada con limitaciones: completó cinco HTTP 404 y cinco fileinfo CLOSED, PCAP 50/50/50 de 6,309 bytes y cero drops. Ninguna ruta exclusiva de preflight aparece en EVE. Su fila coincide con R02/R03 y añade el quinto cruce `seen`. Auditor limpio: 102/145, R04 15/29, 22 coincidencias, cinco cruces y cero inválidas/advertencias. Claude confirmó consistencia documental; Codex verificó artefactos y auditor. Evidencia: `docs/07-dataset-campanas/112-canario-HTTP-404-5-R04.md` y `docs/04-revisiones-claude/2026-08-05-canario-HTTP-404-5-R04.md`. Siguiente: sólo preflight TLS-SESSIONS-20/R04; no hay scoring.

`F1N-TLS-SESSIONS-20-R04` fue aceptada con limitaciones: ejecutó veinte sesiones HTTPS secuenciales con HTTP 200, veinte puertos origen y veinte eventos TLS 1.3 homogéneos. PCAP 433/433/433 de 146,150 bytes, cero drops y 63 paquetes de 500–1500 bytes. Las dos filas nuevas 282/151 pertenecen al mismo episodio y deben mantenerse agrupadas. Suricata incrementó 435 frente a 433, sin causa atribuida. Auditor limpio: 103/145, R04 16/29, 22 coincidencias, cinco cruces y cero inválidas/advertencias. Claude no encontró discrepancias numéricas, pidió conservar en adelante el log crudo desglosado del preflight y autorizó sólo el próximo preflight; Codex verificó artefactos, hashes y auditor. Evidencia: `docs/07-dataset-campanas/113-canario-TLS-SESSIONS-20-R04.md` y `docs/04-revisiones-claude/2026-08-05-canario-TLS-SESSIONS-20-R04.md`. Siguiente: sólo preflight HTTP-MULTI-1/R04; no hay scoring.

`F1N-HTTP-MULTI-1-R04` fue aceptada con limitaciones: completó tres HTTP 200 y tres fileinfo CLOSED, uno por VIP lógica `.10/.11/.12` de una sola VM. PCAP 30/30/30 de 3,369 bytes, Suricata 30/30 y cero drops. Su única fila coincide exactamente con R01–R03 y añade el sexto cruce `seen`. Auditor limpio: 104/145, R04 17/29, 23 coincidencias, seis cruces y cero inválidas/advertencias. El preflight conserva ahora log runtime por gate. Claude no encontró discrepancias y autorizó sólo el próximo preflight; Codex verificó artefactos, hashes y auditor. Evidencia: `docs/07-dataset-campanas/114-canario-HTTP-MULTI-1-R04.md` y `docs/04-revisiones-claude/2026-08-05-canario-HTTP-MULTI-1-R04.md`. Siguiente: sólo preflight HTTP-MULTI-5/R04; no hay scoring.

`F1N-HTTP-MULTI-5-R04` fue aceptada con limitaciones: completó quince HTTP 200 y quince fileinfo CLOSED, cinco por VIP lógica de una sola VM. PCAP 150/150/150 de 16,749 bytes, cero drops y delta Suricata +2. Su única fila coincide exactamente con R01–R03 y añade el séptimo cruce `seen`. Auditor limpio: 105/145, R04 18/29, 24 coincidencias, siete cruces y cero inválidas/advertencias. Claude no encontró discrepancias y autorizó sólo el próximo preflight; Codex verificó artefactos, hashes y auditor. Evidencia: `docs/07-dataset-campanas/115-canario-HTTP-MULTI-5-R04.md` y `docs/04-revisiones-claude/2026-08-05-canario-HTTP-MULTI-5-R04.md`. Siguiente: sólo preflight HTTP-C2/R04; no hay scoring.

`F1N-HTTP-C2-R04` fue aceptada con limitaciones: completó dos descargas concurrentes de 104,857,600 bytes con 9.502 s de solapamiento y agregado 176.41 Mbit/s. PCAP 153,707/153,707/153,707 de 222,580,772 bytes, cero drops y 145,016 paquetes pesados. EVE conserva un flow IPv6 ambiental fuera del filtro PCAP/features y delta Suricata +5. Su única fila es nueva. Auditor limpio: 106/145, R04 19/29, 24 coincidencias, siete cruces y cero inválidas/advertencias. Claude no encontró discrepancias y autorizó sólo el próximo preflight; Codex verificó evidencia primaria, hashes y auditor. Evidencia: `docs/07-dataset-campanas/116-canario-HTTP-C2-R04.md` y `docs/04-revisiones-claude/2026-08-06-canario-HTTP-C2-R04.md`. Siguiente: sólo preflight HTTP-C4/R04; no hay scoring.

`F1N-HTTP-C4-R04` fue aceptada con limitaciones: completó cuatro descargas concurrentes de 104,857,600 bytes, solapadas más de 19.48 s y con agregado 172.01 Mbit/s. PCAP 299,428/299,428/299,428 de 444,446,992 bytes, cero drops y 290,018 paquetes pesados. Tres filas nuevas pertenecen al mismo episodio; delta Suricata +5. Un preflight TCP/22 defectuoso fue invalidado y la repetición completa correcta quedó separada. Auditor limpio: 107/145, R04 20/29, 24 coincidencias, siete cruces y cero inválidas/advertencias. Claude no encontró discrepancias y autorizó sólo el próximo preflight; Codex verificó evidencia primaria, hashes y auditor. Evidencia: `docs/07-dataset-campanas/117-canario-HTTP-C4-R04.md` y `docs/04-revisiones-claude/2026-08-06-canario-HTTP-C4-R04.md`. Siguiente: sólo preflight HTTP-C8/R04; no hay scoring.

`F1N-HTTP-C8-R04` fue aceptada con limitaciones: completó ocho descargas concurrentes de 104,857,600 bytes, solapadas 49.482 s y con agregado 135.54 Mbit/s. Dos PCAP suman 600,491/600,491/600,491 paquetes y 888,812,538 bytes, cero drops y 580,006 paquetes pesados. Seis filas nuevas pertenecen al mismo episodio; delta Suricata +8. Auditor limpio: 108/145, R04 21/29, 24 coincidencias, siete cruces y cero inválidas/advertencias. Claude no encontró discrepancias y autorizó sólo el próximo preflight; Codex verificó evidencia primaria, hashes y auditor. Evidencia: `docs/07-dataset-campanas/118-canario-HTTP-C8-R04.md` y `docs/04-revisiones-claude/2026-08-06-canario-HTTP-C8-R04.md`. Siguiente: sólo preflight TCP-REFUSED-5/R04; no hay scoring.

`F1N-TCP-REFUSED-5-R04` fue aceptada con limitaciones: completó cinco pares SYN–RST/ACK por cinco puertos origen, sin SYN/ACK ni FIN. PCAP 10/10/10 de 824 bytes, cero drops, EVE sólo diez stats y delta Suricata +4. Su fila coincide con R03 y añade el octavo cruce `seen`. Auditor limpio: 109/145, R04 22/29, 25 coincidencias, ocho cruces y cero inválidas/advertencias. Claude no encontró discrepancias y autorizó sólo el próximo preflight; Codex verificó evidencia primaria, hashes y auditor. Evidencia: `docs/07-dataset-campanas/119-canario-TCP-REFUSED-5-R04.md` y `docs/04-revisiones-claude/2026-08-06-canario-TCP-REFUSED-5-R04.md`. Siguiente: sólo preflight TCP-50M/R04; no hay scoring.

`F1N-TCP-50M-R04` fue aceptada con limitaciones: completó 125,042,688 bytes en ambos extremos a 50.013/50.011 Mbit/s, con dos retransmisiones. PCAP 90,440/90,440/90,440 de 132,461,248 bytes, cero drops y 86,816 paquetes pesados. EVE conserva alerta/anomalía L7 permitida y dos flows de preflight emitidos por timeout fuera del PCAP/features; delta Suricata +2. Tres filas nuevas pertenecen al mismo episodio. Auditor limpio: 110/145, R04 23/29, 25 coincidencias, ocho cruces y cero inválidas/advertencias. Claude no encontró discrepancias y autorizó sólo el próximo preflight; Codex verificó evidencia primaria, hashes y auditor. Evidencia: `docs/07-dataset-campanas/120-canario-TCP-50M-R04.md` y `docs/04-revisiones-claude/2026-08-06-canario-TCP-50M-R04.md`. Siguiente: sólo preflight TCP-100M/R04; no hay scoring.

`F1N-TCP-100M-R04` fue aceptada con limitaciones: completó 250,085,376 bytes en ambos extremos a 100.026/100.014 Mbit/s, con dos retransmisiones sin causa atribuida. PCAP 183,003/183,003/183,003 de 265,094,104 bytes, cero drops y 173,629 paquetes pesados. EVE conserva alerta/anomalía L7 permitida y dos flows del preflight fuera del PCAP/features; delta Suricata +2. Tres filas nuevas pertenecen al mismo episodio. Se preservan cinco preflight no capturantes previos al control válido. Auditor limpio: 111/145, R04 24/29, 25 coincidencias, ocho cruces y cero inválidas/advertencias. Claude aceptó con límites y autorizó sólo el próximo preflight; Codex verificó evidencia primaria, hashes y auditor. Evidencia: `docs/07-dataset-campanas/121-canario-TCP-100M-R04.md` y `docs/04-revisiones-claude/2026-08-06-canario-TCP-100M-R04.md`. Siguiente: sólo preflight TCP-200M/R04; no hay scoring.

`F1N-TCP-200M-R04` fue aceptada con limitaciones: completó 500,039,680 bytes en ambos extremos a 200.001/199.997 Mbit/s, con una retransmisión sin causa atribuida. Dos PCAP suman 371,562/371,562/371,562 paquetes y 530,509,500 bytes, cero drops y 347,166 paquetes pesados. EVE contiene alerta/anomalía L7 permitida sin flow autónomo; delta Suricata +2. Tres filas nuevas pertenecen al mismo episodio. Auditor limpio: 112/145, R04 25/29, 25 coincidencias, ocho cruces y cero inválidas/advertencias. Claude confirmó diez artefactos por ruta exacta; Codex verificó conexiones, hashes, recursos y auditor. Evidencia: `docs/07-dataset-campanas/122-canario-TCP-200M-R04.md` y `docs/04-revisiones-claude/2026-08-06-canario-TCP-200M-R04.md`. Siguiente: sólo preflight UDP-10M/R04; no hay scoring.

`F1N-UDP-10M-R04` fue aceptada con limitaciones: transfirió 25,002,616 bytes/17,267 datagramas por extremo, sin pérdida ni reordenamiento; la secuencia binaria `1..17,267` quedó completa. PCAP 17,296/17,296/17,296 de 26,007,319 bytes, cero drops y 17,267 paquetes pesados. EVE conserva un flow DNS del preflight inválido, fuera del PCAP/features; delta Suricata +4. Tres filas nuevas pertenecen al mismo episodio. Auditor limpio: 113/145, R04 26/29, 25 coincidencias, ocho cruces y cero inválidas/advertencias. Claude aceptó; Codex corrigió su conteo EVE, verificó secuencia, hashes y auditor. Evidencia: `docs/07-dataset-campanas/123-canario-UDP-10M-R04.md` y `docs/04-revisiones-claude/2026-08-06-canario-UDP-10M-R04.md`. Siguiente: sólo preflight UDP-25M/R04; no hay scoring.

`F1N-UDP-25M-R04` fue aceptada con limitaciones: transfirió 62,504,368 bytes/43,166 datagramas por extremo, sin pérdida ni reordenamiento; la secuencia binaria `1..43,166` quedó completa. PCAP 43,195/43,195/43,195 de 65,011,217 bytes, cero drops y 43,166 paquetes pesados. EVE conserva dos flows de preflight diferidos fuera del PCAP/features; delta Suricata +4. La fila interior coincide con R02 y eleva correctamente el total a 26 y el subconjunto cruzado a nueve; Claude cuestionó y después cerró esa semántica leyendo el auditor. Auditor limpio: 114/145, R04 27/29, 31 faltantes y cero inválidas/advertencias. Evidencia: `docs/07-dataset-campanas/124-canario-UDP-25M-R04.md` y `docs/04-revisiones-claude/2026-08-06-canario-UDP-25M-R04.md`. Siguiente: sólo preflight UDP-50M/R04; no hay scoring.
